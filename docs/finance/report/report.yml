ReportDefinitions:
  - reportName: ReceiptRegister
    summary: Receipt Register
    version: 1.0.0
    moduleName: finance
    externalService:
      - entity: $.MdmsRes.common-masters.Department
        apiURL: http://egov-mdms-service:8080/egov-mdms-service/v1/_get?moduleName=common-masters&masterName=Department&tenantId=$tenantId
        keyOrder: name,code
        tableName: tbl_def_dept
    sourceColumns:
      - name: receiptnumber
        label: reports.fin.erp.receiptnumber
        type: string
        source: fin
        total: false
      - name: receiptdate
        label: reports.fin.erp.receiptdate
        type: string
        source: fin
        total: false
      - name: service
        label: reports.fin.erp.service
        type: string
        source: fin
        total: false
      - name: department
        label: reports.fin.erp.department
        type: string
        source: fin
        total: false
      - name: cash
        label: reports.fin.erp.cash
        type: number
        source: fin
        total: false
      - name: chequenumber
        label: reports.fin.erp.chequenumber
        type: number
        source: fin
        total: false
      - name: cheque
        label: reports.fin.erp.cheque
        type: number
        source: fin
        total: false
      - name: online
        label: reports.fin.erp.online
        type: number
        source: fin
        total: false
      - name: card
        label: reports.fin.erp.card
        type: number
        source: fin
        total: false
      - name: status
        label: reports.fin.erp.status
        type: string
        source: fin
        total: false
    searchParams:
      - name: fromDate
        label: reports.pt.fromDate
        type: epoch
        source: pt
        isMandatory: false
        searchClause: AND receiptdate >= $fromDate
      - name: toDate
        label: reports.pt.toDate
        type: epoch
        source: pt
        isMandatory: false
        searchClause: AND receiptdate <= $toDate
      - name: dept
        label: Department
        type: singlevaluelist
        pattern: http://egov-mdms-service:8080/egov-mdms-service/v1/_get?tenantId=$tenantid&moduleName=common-masters&masterName=Department|$.MdmsRes.common-masters.Department[*].code|$.MdmsRes.common-masters.Department[*].name
        source: fin
        wrapper: true
        isMandatory: false
        searchClause: And department = $dept

    query: select to_char(to_timestamp(rt.receiptdate/1000),'DD/MM/YYYY') as receiptdate, rt.receiptnumber as receiptnumber, bd.name as Service,(select deptmap_def.name FROM (VALUES tbl_def_dept) AS deptmap_def(name,code) where deptmap_def.code=rt.department) as department, (case when instrumenttype='Cash' then ins.amount end) as cash, (case when instrumenttype='Cheque' then ins.amount when instrumenttype='DD' then ins.amount end) as cheque, (case when instrumenttype='Cheque' then ins.instrumentnumber when instrumenttype='DD' then ins.instrumentnumber end) as chequenumber, (case when instrumenttype='Online' then ins.amount end) as online, (case when instrumenttype='Card' then amount end) as card, rt.status as status from egcl_receiptheader rt, eg_businessdetails bd, egcl_instrumentheader ins where ins.id = (select instrumentheader from egcl_receiptinstrument where receiptheader = rt.id) and rt.tenantid = bd.tenantid and rt.businessdetails = bd.code
orderby: ORDER BY receiptdate DESC NULLS LAST
