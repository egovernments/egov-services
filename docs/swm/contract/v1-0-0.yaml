swagger: '2.0'

info:
  version: "1.0.0"
  title: eGov Solid Waste Management Masters
  description: eGov Solid Waste Management Masters manages all the masters belongs to solid waste management tax.
  contact:
    name: Venkataramana B
    email: venki@egovernments.org
  
basePath: "/swm-services"

x-ui-info:
  $ref: https://raw.githubusercontent.com/egovernments/egov-services/master/docs/swm/contract/vfd-ui-info.yaml
x-module: "swm"

paths:

    /sourcesegregations/_search:
      post:
        summary: Get the list of SourceSegregations
        description: SourceSegregation
        tags: 
          - SourceSegregation
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: codes
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma separated list of codes 
          - name: code
            type: string
            in: query
            description: code of SourceSegregation
          - name: sourceSegregationDate
            type: integer
            format: int64
            in: query
            description: sourceSegregationDate of SourceSegregation
          - name: dumpingGroundCode
            type: string
            in: query
            description: dumping Ground Code of SourceSegregation
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: SourceSegregations(s) retrieved Successfully
            schema:
              $ref: '#/definitions/SourceSegregationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sourcesegregations/_create:
      post:
        summary: Create new SourceSegregations(s)
        description: Create new SourceSegregations(s)
        tags: 
          - SourceSegregation
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: SourceSegregationRequest
            in: body
            description: SourceSegregation Request
            schema:
              $ref: '#/definitions/SourceSegregationRequest'
        responses:
          201:
            description: SourceSegregations(s) created Successfully
            schema:
              $ref: '#/definitions/SourceSegregationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sourcesegregations/_update:
      post:
        summary: Update any of the SourceSegregations
        description: Update any of the SourceSegregations
        tags: 
          - SourceSegregation
        parameters:
          - name: SourceSegregationRequest
            in: body
            description: SourceSegregations Request
            schema:
              $ref: '#/definitions/SourceSegregationRequest'
        responses:
          200:
            description: SourceSegregations updated Successfully
            schema:
              $ref: '#/definitions/SourceSegregationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              

    /sanitationstaffschedules/_search:
      post:
        summary: Get the list of SanitationStaffSchedules
        description: SanitationStaffSchedule
        tags: 
          - SanitationStaffSchedule
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: ids
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma separated list of Ids 
          - name: transactionNo
            type: array
            items: 
              type: string
            in: query
            maxItems: 50
            description: comma separated list of transactionNo
          - name: name
            type: array
            items: 
              type: string
            in: query
            maxItems: 50
            description: comma separated list of SanitationStaff names
          - name: collectionPoints
            type: array
            items: 
              type: string
            in: query
            maxItems: 50
            description: comma separated list of collection points
          - name: dumpingGround
            type: array
            items: 
              type: string
            in: query
            maxItems: 50
            description: comma separated list of Dumping Ground
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: SanitationStaffSchedules(s) retrieved Successfully
            schema:
              $ref: '#/definitions/SanitationStaffScheduleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sanitationstaffschedules/_create:
      post:
        summary: Create new SanitationStaffSchedules(s)
        description: Create new SanitationStaffSchedules(s)
        tags: 
          - SanitationStaffSchedule
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: SanitationStaffScheduleRequest
            in: body
            description: SanitationStaffSchedule Request
            schema:
              $ref: '#/definitions/SanitationStaffScheduleRequest'
        responses:
          201:
            description: SanitationStaffSchedules(s) created Successfully
            schema:
              $ref: '#/definitions/SanitationStaffScheduleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sanitationstaffschedules/_update:
      post:
        summary: Update any of the SanitationStaffSchedules
        description: Update any of the SanitationStaffSchedules
        tags: 
          - SanitationStaffSchedule
        parameters:
          - name: SanitationStaffScheduleRequest
            in: body
            description: SanitationStaffSchedules Request
            schema:
              $ref: '#/definitions/SanitationStaffScheduleRequest'
        responses:
          200:
            description: SanitationStaffSchedules updated Successfully
            schema:
              $ref: '#/definitions/SanitationStaffScheduleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
    
    /sanitationstafftargets/_search:
      post:
        summary: Get the list of SanitationStaffTargets
        description: SanitationStaffTarget
        tags: 
          - SanitationStaffTarget
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: targetNos
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma separated list of targetNos 
          - name: targetNo
            type: string
            in: query
            description: targetNo of SanitationStaffTarget
          - name: routeCode
            type: string
            in: query
            description: routeCode of SanitationStaffTarget
          - name: swmProcessCode
            type: string
            in: query
            description: swmProcessCode of SanitationStaffTarget
          - name: employeeCode
            type: string
            in: query
            description: employeeCode of SanitationStaffTarget
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: SanitationStaffTargets(s) retrieved Successfully
            schema:
              $ref: '#/definitions/SanitationStaffTargetResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sanitationstafftargets/_create:
      post:
        summary: Create new SanitationStaffTargets(s)
        description: Create new SanitationStaffTargets(s)
        tags: 
          - SanitationStaffTarget
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: SanitationStaffTargetRequest
            in: body
            description: SanitationStaffTarget Request
            schema:
              $ref: '#/definitions/SanitationStaffTargetRequest'
        responses:
          201:
            description: SanitationStaffTargets(s) created Successfully
            schema:
              $ref: '#/definitions/SanitationStaffTargetResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sanitationstafftargets/_update:
      post:
        summary: Update any of the SanitationStaffTargets
        description: Update any of the SanitationStaffTargets
        tags: 
          - SanitationStaffTarget
        parameters:
          - name: SanitationStaffTargetRequest
            in: body
            description: SanitationStaffTargets Request
            schema:
              $ref: '#/definitions/SanitationStaffTargetRequest'
        responses:
          200:
            description: SanitationStaffTargets updated Successfully
            schema:
              $ref: '#/definitions/SanitationStaffTargetResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    
    /vehicleschedules/_search:
      post:
        summary: Get the list of Vehicle Schedules
        description: VehicleSchedule
        tags: 
          - VehicleSchedule
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: transactionNos
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma separated list of transactionNos 
          - name: transactionNo
            type: string
            in: query
            description: transactionNo of vehicle schedule
          - name: routeCode
            type: string
            in: query
            description: Route code of vehicle schedule
          - name: regNumber
            type: string
            in: query
            description: regNumber of vehicle schedule
          - name: scheduledFrom
            type: integer
            format: int64 
            in: query
            description: Schedule start date
          - name: scheduledTo
            type: integer
            format: int64 
            in: query
            description: Schedule end date 
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: VehicleSchedules(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VehicleScheduleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehicleschedules/_create:
      post:
        summary: Create new VehicleSchedules(s)
        description: Create new VehicleSchedules(s)
        tags: 
          - VehicleSchedule
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: VehicleScheduleRequest
            in: body
            description: VehicleSchedule Request
            schema:
              $ref: '#/definitions/VehicleScheduleRequest'
        responses:
          201:
            description: VehicleSchedule(s) created Successfully
            schema:
              $ref: '#/definitions/VehicleScheduleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehicleschedules/_update:
      post:
        summary: Update any of the VehicleSchedules
        description: Update any of the VehicleSchedules
        tags: 
          - VehicleSchedule
        parameters:
          - name: VehicleScheduleRequest
            in: body
            description: VehicleSchedules Request
            schema:
              $ref: '#/definitions/VehicleScheduleRequest'
        responses:
          200:
            description: VehicleSchedules updated Successfully
            schema:
              $ref: '#/definitions/VehicleScheduleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
    
    /refillingpumpstations/_search:
      post:
        summary: Get the list of Refilling Pump Stations
        description: RefillingPumpStation
        tags: 
          - RefillingPumpStation
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: code
            type: string
            in: query
            description: code of pump station
          - name: name
            type: string
            in: query
            description: name of pump station
          - name: quantity
            type: number
            format: double
            in: query
            description: quantity of pump station
          - name: locationCode
            type: string
            in: query
            description: location code of pump statio
          - name: typeoffuelCode
            type: string
            in: query
            description: typeoffuel code of pump station
          - name: typeofpumpCode
            type: string
            in: query
            description: typeofpump code of pump station
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: RefillingPumpStations(s) retrieved Successfully
            schema:
              $ref: '#/definitions/RefillingPumpStationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /refillingpumpstations/_create:
      post:
        summary: Create new RefillingPumpStations(s)
        description: Create new RefillingPumpStations(s)
        tags: 
          - RefillingPumpStation
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: RefillingPumpStationRequest
            in: body
            description: RefillingPumpStation Request
            schema:
              $ref: '#/definitions/RefillingPumpStationRequest'
        responses:
          201:
            description: RefillingPumpStations(s) created Successfully
            schema:
              $ref: '#/definitions/RefillingPumpStationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /refillingpumpstations/_update:
      post:
        summary: Update any of the RefillingPumpStations
        description: Update any of the RefillingPumpStations
        tags: 
          - RefillingPumpStation
        parameters:
          - name: RefillingPumpStationRequest
            in: body
            description: RefillingPumpStations Request
            schema:
              $ref: '#/definitions/RefillingPumpStationRequest'
        responses:
          200:
            description: RefillingPumpStations updated Successfully
            schema:
              $ref: '#/definitions/RefillingPumpStationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
    
    /vendorcontracts/_search:
      post:
        summary: Get the list of Vendor Contract records
        description: Vendor Contract
        tags: 
          - VendorContract
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: contractNos
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma separated list of contractNo 
          - name: contractNo
            type: string
            in: query
            description: contract number of contract
          - name: vendorNo
            type: string
            in: query
            description: vendor number of contract
          - name: contractPeriodFrom
            type: integer
            format: int64 
            in: query
            description: Contract period start date
          - name: contractPeriodto
            type: integer
            format: int64 
            in: query
            description: Contract period end date
          - name: contractDate
            type: integer
            format: int64 
            in: query
            description: Contract period date
          - name: securityDeposit
            type: number
            format: double 
            in: query
            description: security Deposit of contract 
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: VendorContracts(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VendorContractResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vendorcontracts/_create:
      post:
        summary: Create new Vendor Contract(s) records
        description: Create new Vendor Contract(s) records
        tags: 
          - VendorContract
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: VendorContractRequest
            in: body
            description: VendorContract Request
            schema:
              $ref: '#/definitions/VendorContractRequest'
        responses:
          201:
            description: VendorContracts(s) created Successfully
            schema:
              $ref: '#/definitions/VendorContractResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vendorcontracts/_update:
      post:
        summary: Update any of the VendorContract record
        description: Update any of the VendorContract record
        tags: 
          - VendorContract
        parameters:
          - name: VendorContractRequest
            in: body
            description: VendorContracts Request
            schema:
              $ref: '#/definitions/VendorContractRequest'
        responses:
          200:
            description: Vendor Contract record updated Successfully
            schema:
              $ref: '#/definitions/VendorContractResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
    
    /vehiclemaintenances/_search:
      post:
        summary: Get the list of Vehicle Maintenance records
        description: Vehicle Maintenance
        tags: 
          - VehicleMaintenance
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: codes
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma separated list of codes 
          - name: regNumber
            type: string
            in: query
            description: registration number of vehicle
          - name: vehicleTypeCode
            type: string
            in: query
            description: code of vehicle Type
          - name: code
            type: string
            in: query
            description: code of vehicle maintyenance
          - name: downtimeforMaintenance
            type: integer
            format: int64
            in: query
            description: downtimeforMaintenance of vehicle maintyenance
          - name: maintenanceAfter
            type: integer
            format: int64
            in: query
            description: maintenanceAfter of vehicle maintyenance
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: VehicleMaintenances(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VehicleMaintenanceResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
    
    /vehiclemaintenances/_create:
      post:
        summary: Create new Vehicle Maintenance(s) records
        description: Create new Vehicle Maintenance(s) records
        tags: 
          - VehicleMaintenance
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: VehicleMaintenanceRequest
            in: body
            description: VehicleMaintenance Request
            schema:
              $ref: '#/definitions/VehicleMaintenanceRequest'
        responses:
          201:
            description: VehicleMaintenances(s) created Successfully
            schema:
              $ref: '#/definitions/VehicleMaintenanceResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehiclemaintenances/_update:
      post:
        summary: Update any of the VehicleMaintenance record
        description: Update any of the VehicleMaintenance record
        tags: 
          - VehicleMaintenance
        parameters:
          - name: VehicleMaintenanceRequest
            in: body
            description: VehicleMaintenances Request
            schema:
              $ref: '#/definitions/VehicleMaintenanceRequest'
        responses:
          200:
            description: Vehicle Maintenance record updated Successfully
            schema:
              $ref: '#/definitions/VehicleMaintenanceResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
    
    /vendors/_search:
      post:
        summary: Get the list of vendor
        description: vendor
        tags: 
          - Vendor
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: vendorNos
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma seperated list of vendorNo's
          - name: vendorNo
            type: string
            in: query
            description: vendorNo of the Vendor
          - name: name
            type: string
            in: query
            description: name of the Vendor
          - name: registrationNo
            type: string
            in: query
            description: registrationNo of the Vendor
            maxLength: 256
          - name: supplier
            type: string
            in: query
            description: supplier code of the Vendor
            maxLength: 256
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
          - name: sortBy
            type: string
            in: query
            description: |
              This takes any field from the Object seperated by comma and asc,desc keywords.  
              example name asc,code desc or name,code or name,code desc 
        responses:
          200:
            description: Vendor(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VendorResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vendors/_create:
      post:
        summary: Create new vendor(s)
        description: Create new vendor(s)
        tags: 
          - Vendor
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: vendorRequest
            in: body
            description: Vendor Request
            schema:
              $ref: '#/definitions/VendorRequest'
        responses:
          201:
            description: Vendor(s) created Successfully
            schema:
              $ref: '#/definitions/VendorResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vendors/_update:
      post:
        summary: Update any of the vendor
        description: Update any of the vendor
        tags: 
          - Vendor
        parameters:
          - name: vendorRequest
            in: body
            description: Vendor Request
            schema:
              $ref: '#/definitions/VendorRequest'
        responses:
          200:
            description: Vendor updated Successfully
            schema:
              $ref: '#/definitions/VendorResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /routes/_search:
      post:
        summary: Get the list of route
        description: route
        tags: 
          - Route
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: codes
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma seperated list of codes
          - name: code
            type: string
            in: query
            description: code of the Route
          - name: name
            type: string
            in: query
            description: name of the Route
          - name: collectionTypeCode
            type: string
            in: query
            description: collectionType of the Route
            maxLength: 256
          - name: startingCollectionPointCode
            type: string
            in: query
            description: endingCollectionPoint code of the Route
            maxLength: 256
          - name: endingCollectionPointCode
            type: string
            in: query
            description: endingCollectionPoint code of the Route
            maxLength: 256
          - name: isEndingDumpingGround
            type: boolean
            in: query
            description: if true will only return ending collection point or Dumping ground in route response.
          - name: endingDumpingGroundPointCode
            type: string
            in: query
            description: endingDumpingGroundPoint code of the Route
            maxLength: 256
          - name: distance
            type: number
            format: double
            in: query
            description: distance  of the Route
          - name: gabageEstimate
            type: number
            format: double
            in: query
            description: gabageEstimate  of the Route
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
          - name: sortBy
            type: string
            in: query
            description: |
              This takes any field from the Object seperated by comma and asc,desc keywords.  
              example name asc,code desc or name,code or name,code desc 
        responses:
          200:
            description: Route(s) retrieved Successfully
            schema:
              $ref: '#/definitions/RouteResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /routes/_create:
      post:
        summary: Create new route(s)
        description: Create new route(s)
        tags: 
          - Route
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: routeRequest
            in: body
            description: Route Request
            schema:
              $ref: '#/definitions/RouteRequest'
        responses:
          201:
            description: Route(s) created Successfully
            schema:
              $ref: '#/definitions/RouteResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /routes/_update:
      post:
        summary: Update any of the route
        description: Update any of the route
        tags: 
          - Route
        parameters:
          - name: routeRequest
            in: body
            description: Route Request
            schema:
              $ref: '#/definitions/RouteRequest'
        responses:
          200:
            description: Route updated Successfully
            schema:
              $ref: '#/definitions/RouteResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /collectionpoints/_search:
      post:
        summary: Get the list of collectionPoint
        description: collectionPoint
        tags: 
          - CollectionPoint
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: codes
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma seperated list of codes
          - name: name
            type: string
            in: query
            description: name of the CollectionPoint
            maxLength: 256
          - name: locationCode
            type: string
            in: query
            description: location code of the CollectionPoint
            maxLength: 256  
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
          - name: sortBy
            type: string
            in: query
            description: |
              This takes any field from the Object seperated by comma and asc,desc keywords.  
              example name asc,code desc or name,code or name,code desc 
        responses:
          200:
            description: CollectionPoint(s) retrieved Successfully
            schema:
              $ref: '#/definitions/CollectionPointResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /collectionpoints/_create:
      post:
        summary: Create new collectionPoint(s)
        description: Create new collectionPoint(s)
        tags: 
          - CollectionPoint
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: collectionPointRequest
            in: body
            description: CollectionPoint Request
            schema:
              $ref: '#/definitions/CollectionPointRequest'
        responses:
          201:
            description: CollectionPoint(s) created Successfully
            schema:
              $ref: '#/definitions/CollectionPointResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /collectionpoints/_update:
      post:
        summary: Update any of the collectionPoint
        description: Update any of the collectionPoint
        tags: 
          - CollectionPoint
        parameters:
          - name: collectionPointRequest
            in: body
            description: CollectionPoint Request
            schema:
              $ref: '#/definitions/CollectionPointRequest'
        responses:
          200:
            description: CollectionPoint updated Successfully
            schema:
              $ref: '#/definitions/CollectionPointResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehicles/_search:
      post:
        summary: Get the list of vehicle
        description: vehicle
        tags: 
          - Vehicle
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: ids
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma seperated list of Ids
          - name: vehicleTypeCode
            type: string
            format: singleValueList
            in: query
            description: vehicleType code of the Vehicle
            maxLength: 256
          - name: fuelTypeCode
            type: string
            format: singleValueList
            in: query
            description: fuelTypeCode code of the Vehicle
            maxLength: 256  
          - name: regNumber
            type: string
            in: query
            description: regNumber of the Vehicle
            maxLength: 12
          - name: engineSrNumber
            type: string
            in: query
            description: engineSrNumber of the Vehicle
            maxLength: 256
          - name: chassisSrNumber
            type: string
            in: query
            description: chassisSrNumber of the Vehicle
            maxLength: 256
          - name: model
            type: string
            in: query
            description: model of the Vehicle
            maxLength: 256
          - name: ulbOwnedVehicle
            type: boolean
            format: checkbox
            in: query
            description: ulbOwnedVehicle of the Vehicle
          - name: vendorName
            type: string
            format: singleValueList
            in: query
            description: vendor Name of the Vehicle
            maxLength: 265
          - name: purchaseDate
            type: integer
            format: int64
            in: query
            description: purchaseDate of the Vehicle
          - name: purchaseYear
            type: string
            format: singleValueList
            in: query
            description: year Of Purchase of the Vehicle
            maxLength: 265
          - name: insuranceNumber
            type: string
            in: query
            description: insuranceNumber of the Vehicle
          - name: insuranceValidityDate
            type: integer
            format: int64
            in: query
            description: insuranceValidityDate of the Vehicle 
          - name: isUnderWarranty
            type: boolean
            format: checkbox
            in: query
            description: isUnderWarranty of the Vehicle
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
          - name: sortBy
            type: string
            in: query
            description: |
              This takes any field from the Object seperated by comma and asc,desc keywords.  
              example name asc,code desc or name,code or name,code desc 
        responses:
          200:
            description: Vehicle(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VehicleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehicles/_create:
      post:
        summary: Create new vehicle(s)
        description: Create new vehicle(s)
        tags: 
          - Vehicle
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: vehicleRequest
            in: body
            description: Vehicle Request
            schema:
              $ref: '#/definitions/VehicleRequest'
        responses:
          201:
            description: Vehicle(s) created Successfully
            schema:
              $ref: '#/definitions/VehicleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehicles/_update:
      post:
        summary: Update any of the vehicle
        description: Update any of the vehicle
        tags: 
          - Vehicle
        parameters:
          - name: vehicleRequest
            in: body
            description: Vehicle Request
            schema:
              $ref: '#/definitions/VehicleRequest'
        responses:
          200:
            description: Vehicle updated Successfully
            schema:
              $ref: '#/definitions/VehicleResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /vehiclefuellingdetails/_search:
      post:
        summary: Get the list of vehiclefuellingdetails
        description: vehiclefuellingdetails
        tags: 
          - VehicleFuellingDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: ids
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma seperated list of Ids
          - name: transactionNo
            type: string
            in: query
            description: transactionNo of the VehicleFuellingDetails
            maxLength: 265
          - name: transactionDate
            type: integer
            format: int64
            in: query
            description: transactionDate of the VehicleFuellingDetails
            maxLength: 265
          - name: regNumber
            type: string
            format: singleValueList
            in: query
            description: vehicleRegNo  of the VehicleFuellingDetails
            maxLength: 265
          - name: vehicleReadingDuringFuelling
            type: string
            in: query
            description: vehicleReadingDuringFuelling of the VehicleFuellingDetails
          - name: refuellingStationName
            type: string
            format: singleValueList
            in: query
            description: refuellingStation name of the VehicleFuellingDetails
            maxLength: 265
          - name: fuelFilled
            type: number
            format: double
            in: query
            description: fuelFilled of the VehicleFuellingDetails
            maxLength: 265
          - name: fuelTypeCode
            type: string
            in: query
            description: fuelType Code of the VehicleFuellingDetails
            maxLength: 265
          - name: totalCostIncurred
            type: number
            format: double
            in: query
            description: totalCostIncurred of the VehicleFuellingDetails
          - name: receiptNo
            type: string
            in: query
            description: receiptNo of the VehicleFuellingDetails
            maxLength: 265
          - name: receiptDate
            type: integer
            format: int64
            in: query
            description: receiptDate of the VehicleFuellingDetails
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
          - name: sortBy
            type: string
            in: query
            description: |
              This takes any field from the Object seperated by comma and asc,desc keywords.  
              example name asc,code desc or name,code or name,code desc 
        responses:
          200:
            description: VehicleFuellingDetails(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VehicleFuellingDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehiclefuellingdetails/_create:
      post:
        summary: Create new vehiclefuellingdetails(s)
        description: Create new vehiclefuellingdetails(s)
        tags: 
          - VehicleFuellingDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: vehicleFuellingDetailsRequest
            in: body
            description: VehicleFuellingDetails Request
            schema:
              $ref: '#/definitions/VehicleFuellingDetailsRequest'
        responses:
          201:
            description: VehicleFuellingDetails(s) created Successfully
            schema:
              $ref: '#/definitions/VehicleFuellingDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehiclefuellingdetails/_update:
      post:
        summary: Update any of the vehiclefuellingdetails
        description: Update any of the vehiclefuellingdetails
        tags: 
          - VehicleFuellingDetails
        parameters:
          - name: vehicleFuellingDetailsRequest
            in: body
            description: VehicleFuellingDetails Request
            schema:
              $ref: '#/definitions/VehicleFuellingDetailsRequest'
        responses:
          200:
            description: VehicleFuellingDetails updated Successfully
            schema:
              $ref: '#/definitions/VehicleFuellingDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehiclemaintenancedetails/_search:
      post:
        summary: Get the list of vehiclemaintenancedetails
        description: vehiclemaintenancedetails
        tags: 
          - VehicleMaintenanceDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: ids
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma seperated list of Ids 
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: VehicleMaintenanceDetails(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VehicleMaintenanceDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehiclemaintenancedetails/_create:
      post:
        summary: Create new vehiclemaintenancedetails(s)
        description: Create new vehiclemaintenancedetails(s)
        tags: 
          - VehicleMaintenanceDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: vehicleMaintenanceDetailsRequest
            in: body
            description: VehicleMaintenanceDetails Request
            schema:
              $ref: '#/definitions/VehicleMaintenanceDetailsRequest'
        responses:
          201:
            description: VehicleMaintenanceDetails(s) created Successfully
            schema:
              $ref: '#/definitions/VehicleMaintenanceDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehiclemaintenancedetails/_update:
      post:
        summary: Update any of the vehiclemaintenancedetails
        description: Update any of the vehiclemaintenancedetails
        tags: 
          - VehicleMaintenanceDetails
        parameters:
          - name: vehicleMaintenanceDetailsRequest
            in: body
            description: VehicleMaintenanceDetails Request
            schema:
              $ref: '#/definitions/VehicleMaintenanceDetailsRequest'
        responses:
          200:
            description: VehicleMaintenanceDetails updated Successfully
            schema:
              $ref: '#/definitions/VehicleMaintenanceDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehicletripsheetdetails/_search:
      post:
        summary: Get the list of vehicletripsheetdetails
        description: vehicletripsheetdetails
        tags: 
          - VehicleTripSheetDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: ids
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma seperated list of Ids 
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: VehicleTripSheetDetails(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VehicleTripSheetDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehicletripsheetdetails/_create:
      post:
        summary: Create new vehicletripsheetdetails(s)
        description: Create new vehicletripsheetdetails(s)
        tags: 
          - VehicleTripSheetDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: vehicleTripSheetDetailsRequest
            in: body
            description: VehicleTripSheetDetails Request
            schema:
              $ref: '#/definitions/VehicleTripSheetDetailsRequest'
        responses:
          201:
            description: VehicleTripSheetDetails(s) created Successfully
            schema:
              $ref: '#/definitions/VehicleTripSheetDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vehicletripsheetdetails/_update:
      post:
        summary: Update any of the vehicletripsheetdetails
        description: Update any of the vehicletripsheetdetails
        tags: 
          - VehicleTripSheetDetails
        parameters:
          - name: vehicleTripSheetDetailsRequest
            in: body
            description: VehicleTripSheetDetails Request
            schema:
              $ref: '#/definitions/VehicleTripSheetDetailsRequest'
        responses:
          200:
            description: VehicleTripSheetDetails updated Successfully
            schema:
              $ref: '#/definitions/VehicleTripSheetDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sourcesegregation/_search:
      post:
        summary: Get the list of sourcesegregation
        description: sourcesegregation
        tags: 
          - SourceSegregation
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: ids
            type: array
            items: 
              type: integer
              format: int64
            in: query
            maxItems: 50
            description: comma seperated list of Ids 
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: SourceSegregation(s) retrieved Successfully
            schema:
              $ref: '#/definitions/SourceSegregationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sourcesegregation/_create:
      post:
        summary: Create new sourcesegregation(s)
        description: Create new sourcesegregation(s)
        tags: 
          - SourceSegregation
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: sourceSegregationRequest
            in: body
            description: SourceSegregation Request
            schema:
              $ref: '#/definitions/SourceSegregationRequest'
        responses:
          201:
            description: SourceSegregation(s) created Successfully
            schema:
              $ref: '#/definitions/SourceSegregationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sourcesegregation/_update:
      post:
        summary: Update any of the sourcesegregation
        description: Update any of the sourcesegregation
        tags: 
          - SourceSegregation
        parameters:
          - name: sourceSegregationRequest
            in: body
            description: SourceSegregation Request
            schema:
              $ref: '#/definitions/SourceSegregationRequest'
        responses:
          200:
            description: SourceSegregation updated Successfully
            schema:
              $ref: '#/definitions/SourceSegregationResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vendorpaymentdetails/_search:
      post:
        summary: Get the list of vendorpaymentdetails
        description: vendorpaymentdetails
        tags: 
          - VendorPaymentDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: paymentNos
            type: string
            in: query
            description: comma seperated list of paymentNos 
          - name: paymentNo
            type: string
            in: query
            description: paymentNo of the PaymentDetails 
          - name: invoiceFromDate
            type: string
            in: query
            description: search for from invoice date specified
          - name: invoiceToDate
            type: string
            in: query
            description: search for till invoice date specified
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: VendorPaymentDetails(s) retrieved Successfully
            schema:
              $ref: '#/definitions/VendorPaymentDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vendorpaymentdetails/_create:
      post:
        summary: Create new vendorpaymentdetails(s)
        description: Create new vendorpaymentdetails(s)
        tags: 
          - VendorPaymentDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: vendorPaymentDetailsRequest
            in: body
            description: VendorPaymentDetails Request
            schema:
              $ref: '#/definitions/VendorPaymentDetailsRequest'
        responses:
          201:
            description: VendorPaymentDetails(s) created Successfully
            schema:
              $ref: '#/definitions/VendorPaymentDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /vendorpaymentdetails/_update:
      post:
        summary: Update any of the vendorpaymentdetails
        description: Update any of the vendorpaymentdetails
        tags: 
          - VendorPaymentDetails
        parameters:
          - name: vendorPaymentDetailsRequest
            in: body
            description: VendorPaymentDetails Request
            schema:
              $ref: '#/definitions/VendorPaymentDetailsRequest'
        responses:
          200:
            description: VendorPaymentDetails updated Successfully
            schema:
              $ref: '#/definitions/VendorPaymentDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /paymentdetails/_search:
      post:
        summary: Get the list of paymentdetails
        description: paymentdetails
        tags: 
          - PaymentDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo'
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: code
            type: string
            in: query
            description: code of the PaymentDetails
          - name: codes
            type: string
            in: query
            description: comma seperated list of Codes 
          - name: paymentNo
            type: string
            in: query
            description: paymentNo of the PaymentDetails
          - name: voucherNumber
            type: string
            in: query
            description: voucherNumber of the PaymentDetails
          - name: instrumentNumber
            type: string
            in: query
            description: instrumentNumber of the PaymentDetails
            maxLength: 265
          - $ref: '#/parameters/pageSize'
          - $ref: '#/parameters/offSet'
        responses:
          200:
            description: PaymentDetails(s) retrieved Successfully
            schema:
              $ref: '#/definitions/PaymentDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /paymentdetails/_create:
      post:
        summary: Create new paymentdetails(s)
        description: Create new paymentdetails(s)
        tags: 
          - PaymentDetails
        parameters:
          - $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId'
          - name: paymentDetailsRequest
            in: body
            description: PaymentDetails Request
            schema:
              $ref: '#/definitions/PaymentDetailsRequest'
        responses:
          201:
            description: PaymentDetails(s) created Successfully
            schema:
              $ref: '#/definitions/PaymentDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /paymentdetails/_update:
      post:
        summary: Update any of the paymentdetails
        description: Update any of the paymentdetails
        tags: 
          - PaymentDetails
        parameters:
          - name: paymentDetailsRequest
            in: body
            description: PaymentDetails Request
            schema:
              $ref: '#/definitions/PaymentDetailsRequest'
        responses:
          200:
            description: PaymentDetails updated Successfully
            schema:
              $ref: '#/definitions/PaymentDetailsResponse'
          400:
            description: Invalid Input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

definitions:

    CollectionPoint:
      type: object
      description: Captures location of collection point and expected garbage collection against one or more collection types. Identities of bins associated with respective collection points are also maintained in this object 
      properties:
        code:
          type: string
          description: Unique Identifier of the CollectionPoint
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the CollectionPoint
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the CollectionPoint
          maxLength: 128
          minLength: 1
        location:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/egov-location/contracts/v1-0-0.yml#/definitions/Boundary'
        binDetails:
          type: array
          items:
            $ref: "#/definitions/BinDetails"
        collectionPointDetails:
          type: array
          items:
            $ref: "#/definitions/CollectionPointDetails"
          minItems: 1
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - name
      - location
      
    CollectionPointRequest:
      description: Contract class to receive request. Array of CollectionPoint items are used in case of create, whereas single CollectionPoint item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        collectionPoints:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/CollectionPoint'
    CollectionPointResponse:
      description: Contract class to send response. Array of CollectionPoint items are used in case of search results or response for create, whereas single CollectionPoint item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        collectionPoints:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/CollectionPoint'

    BinDetails:
      type: object
      properties:
        id:
          type: string
          description: Unique Identifier of the BinIdDetails
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the BinIdDetails
          maxLength: 128
          minLength: 1
        assetOrBinId:
          type: string
          description: asset or BIN Id of the Collection Point. If defined, shall be unique within ULB
          maxLength: 256
          minLength: 1
        rfidAssigned:
          type: boolean
          format: checkbox
          description: Option should be selected if RFID is assigned
        rfid:
          type: string
          description: rfid of the Collection Point. If rfidAssigned is TRUE, this field is mandatory
          maxLength: 256
          minLength: 1
        latitude:
          type: number 
          format: double
          description: latitude of the Collection Point. e.g. 12.9716. Latitudes range from -90 to 90. Positive latitude is above the equator (N), and negative latitude is below the equator (S).
          example: 38.544907
        longitude:
          type: number 
          format: double
          description: longitude of the Collection Point. e.g. 77.5946. Longitudes range from -180 to 180. Positive longitude is east of the prime meridian, while negative longitude is west of the prime meridian
          example: -121.740517
      required:
      - tenantId
      - assetOrBinId
      
    CollectionPointDetails:
      type: object
      description: Sub-object under collection point. Creates a mapping between collection types, expected garbage collection and collection points.
      properties:
        id:
          type: string
          description: Unique Identifier of the CollectionPointDetails
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the CollectionPointDetails
          maxLength: 128
          minLength: 4
        collectionType:
          $ref: '#/definitions/CollectionType'
          format: singleValueList
        garbageEstimate:
          type: number
          format: double
          description: Quantity of garbage (in tonnes) that is expected at collection point
        description:
          type: string
          description: ULB Admin can enter the remarkks, if any
          maxLength: 300
          minLength: 0
      required:
      - tenantId
      - collectionType
      - garbageEstimate
      
    Route:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the Route
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the Route
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the Route. Shall be unique within ULB
          maxLength: 128
          minLength: 1
        collectionType:  
          $ref: "#/definitions/CollectionType"
        collectionPoints:
          type: array
          items:
            $ref: '#/definitions/RouteCollectionPointMap'
          description: List of RouteCollectionPointMap on the Route. This will have all the selected collection points based on Ward / zone / street.
        totalDistance:
          type: number
          format: double
          description: it captures total distance (in KMs) of the CollectionPoint, starting from First point to ending point. e.g. 10.75
          minimum: 0
          maximum: 500
        totalGarbageEstimate:
          type: number
          format: double
          description: it captures total garbage collection (in tonnes) for all the CollectionPoint. e.g. 4.26  
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - name
      - collectionType
      - collectionPoints
      - totalDistance
      - totalGarbageEstimate
      
    RouteCollectionPointMap:
      type: object
      properties:
        id:
          type: string
          description: Unique Identifier of the RouteCollectionPointMap
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the RouteCollectionPointMap
          maxLength: 128
          minLength: 1
        route:
          type: string
          description: route id of the RouteCollectionPointMap
          maxLength: 128
          minLength: 1
        isStartingCollectionPoint:
          type: boolean
          description: is Starting Collection Point id of the RouteCollectionPointMap
        isEndingCollectionPoint:
          type: boolean
          description: is Starting Collection Point id of the RouteCollectionPointMap
        collectionPoint:
          description: collectio point will be shown based on what collection type  and what location has been selected.
          $ref: "#/definitions/CollectionPoint"
        dumpingGround:
          description: This dropdown will show the list of dumping grounds and processing plants. Either endPoint or Dumping ground can be selected. At least one has to be selected
          $ref: "#/definitions/DumpingGround"
          format: singleValueList
        distance:
          type: number
          format: double
          description: it captures distance (in KMs) of the CollectionPoint, starting from First point to ending point. e.g. 10.75
          minimum: 0
          maximum: 500
        garbageEstimate:
          type: number
          format: double
          description: it captures expected garbage collection (in tonnes) for a CollectionPoint. e.g. 4.26
      required:
      - tenantId
      - route
      
    RouteRequest:
      description: Contract class to receive request. Array of Route items are used in case of create, whereas single Route item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        routes:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/Route'
            
    RouteResponse:
      description: Contract class to send response. Array of Route items are used in case of search results or response for create, whereas single Route item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        routes:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/Route'
    
    FuelType:
      type: object
      description: Defines the type of fuel. e.g.  Diesel, Petrol etc
      properties:
        tenantId:
          type: string
          description: tenant id of the FuelType
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the FuelType. e.g  Diesel, Petrol etc
          maxLength: 128
          minLength: 1
        code:
          type: string
          description: code of the FuelType. Unique code assigned to the FuelType. Shall be of at least 2 digits. fuelCode will be system generated
          maxLength: 128
          minLength: 1
      required:
      - tenantId
      - code
      - name
      
    VehicleType:
      type: object
      properties:
        tenantId:
          type: string
          description: tenant id of the VehicleType
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the VehicleType
          maxLength: 128
          minLength: 1
        code:
          type: string
          description: code of the VehicleType
          maxLength: 128
          minLength: 1
      required:
      - tenantId
      - code
      - name
      
    Vehicle:
      type: object
      description: In order to identify each and every vehicle, this master defines attributes of individual vehicle. e.g License plate number, Chassis number etc
      properties:
        tenantId:
          type: string
          description: tenant id of the Vehicle
          maxLength: 128
          minLength: 1
        regNumber:
          type: string
          description: registration Number of the Vehicle. Should be unique across the platform. Special characters are not allowed. Space not allowed. 
          maxLength: 22
          minLength: 1
          example: KA519876
        vehicleType:
          $ref: "#/definitions/VehicleType"
        purchaseInfo:
          $ref: "#/definitions/PurchaseInfo"
        fuelType:
          $ref: "#/definitions/FuelType"
        operatorsReq:
          type: integer
          format: int64
          description: number Of Persons Required to operate the Vehicle. Shall be >= 0
          minimum: 0
          maximum: 15
          example: 3
        driver:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/hr/contracts/hr-employee/v1-0-0.yml#/definitions/Employee'
          description: name of the driver who will drive the Vehicle. Driver name shall be fetched from HRMS. Assumption-> One driver shall be assigned to one vehicle only. Raise an visual alert if driver is being assigned to more than 1 vehicle but allow the assignment.
        vendor:
          $ref: "#/definitions/Vendor"
          description: name of the vendor who supplied the Vehicle. If name is not provide, vehicle is considered as ULB vehicle
        manufacturingDetails:
          $ref: '#/definitions/ManufacturingDetails'
        insuranceDetails:
          $ref: '#/definitions/InsuranceDetails'
          description: Captures insurance details of vehicle. Current and previuos insurance information
        kilometers:
          type: integer
          format: int64
          maximum: 100000
          minimum: 0
          description: Captures for how many more KMs from the date of creation of thie entry, warranty is applicable.
          example: 24502
        endOfWarranty:
          type: integer
          format: int64
          description: If date provided is past date, vehicle is not under warranty
          x-displayformat: 26/10/2017
        remarks:
          type: string
          description: ULB admin can enter a comment 
          maxLength: 300
          minLength: 15
        isVehicleUnderWarranty:
          type: boolean
          description: true if value is present for both kilometers and endOfWarranty
        isVehicleUlbOwned:
          type: boolean
          description: true if value is present for vendor
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - regNumber
      - vehicleType
      - purchaseInfo
      - fuelType
      - manufacturingDetails
      - operatorsReq
      - driver
 
    PurchaseInfo:
      type: object
      description: Purchase related information of the vehicle. e.g registration number, purchase price etc
      properties:
        purchaseDate:
          type: integer
          format: int64
          x-displayformat: 20/10/2015
          description: Purchase date of the vehicle. # We might have to intgrate with Asset management module to fetch vehicle related record.
        price:
          type: number 
          format: double
          maximum: 10000000
          minimum: 1
          description: purchase price of the Vehicle
          example: 367000.00
        sourceOfPurchase:
          type: string
          description: source of purchase of the Vehicle
          maxLength: 256
          minLength: 0
      required:
      - purchaseDate
      - price
    
    
    ManufacturingDetails:
      type: object
      description: Captures manufacturing related details of the vehicle. e.g. engine number, chassis number etc
      properties:
        engineSrNumber:
          type: string
          description: engine serial number of the Vehicle. Should be unique across the platform. Special characters are allowed. Space not allowed
          maxLength: 256
          minLength: 1
          example: 1234/abvd-56e5353
        chassisSrNumber:
          type: string
          description: chassis serial number of the Vehicle. Should be unique across the platform. Special characters are allowed. Space not allowed
          maxLength: 256
          minLength: 1
          example: 1234/abvd-56e5353
        vehicleCapacity:
          type: number
          format: double
          description: capacity of the Vehicle in Tonnes. Shall be > 0
          minimum: 0
          exclusiveMinimum: true
          example: 0.24
        model:
          type: string
          description: Make / Model / Manufaturer of the Vehicle. / special character is allowed. Space is not allowed
          maxLength: 256
          minLength: 1
          example: Dzire/LDi/Maruti
      required:
      - engineSrNumber
      - chassisSrNumber
      - vehicleCapacity
      - model
      
    
    InsuranceDetails:
      type: object
      description: Captures Insurance details of the vehicle
      properties:
        insuranceNumber:
          type: string
          description: Current Insurance number of vehicle
          maxLength: 256
          minLength: 1
        insuranceValidityDate:
          type: integer
          format: int64
          x-displayformat: 26/05/2018
          description: Current or Futrure date at which insurance is going to expire
        insuranceDocuments:
          $ref: '#/definitions/Document'
      required:
      - insuranceNumber
      - insuranceValidityDate
    
    
    
    VehicleRequest:
      description: Contract class to receive request. Array of Vehicle items are used in case of create, whereas single Vehicle item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vehicles:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/Vehicle'
            
    VehicleResponse:
      description: Contract class to send response. Array of Vehicle items are used in case of search results or response for create, whereas single Vehicle item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vehicles:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/Vehicle'
      
    WasteType:
      type: object
      properties:
        tenantId:
          type: string
          description: tenant id of the WasteType
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the WasteType. e.g. Dry, wet or medicinal waste etc. Special chararcter not allowed. Space allowed
          maxLength: 128
          minLength: 1
        code:
          type: string
          description: code of the WasteType. e.g. Dry, wet or medicinal waste etc. Special chararcter not allowed. Space allowed
          maxLength: 128
          minLength: 1
      required:
      - tenantId
      - code
      - name
      
    WasteSubType:
      type: object
      description: waste subtype will be defined undedr waste type
      properties:
        tenantId:
          type: string
          description: tenant id of the WasteSubType
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the WasteSubType. e.g. Household or market waste. Waste Subtype is a subset of waste type. Special chararcter not allowed. Space allowed
          maxLength: 128
          minLength: 1
        code:
          type: string
          description: code of the WasteSubType. e.g. Household or market waste. Waste Subtype is a subset of waste type. Special chararcter not allowed. Space allowed
          maxLength: 128
          minLength: 1
        wasteType:
          $ref: "#/definitions/WasteType"
      required:
      - tenantId
      - code
      - wasteType
      - name
      
    DumpingGround:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the DumpingGround
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the DumpingGround
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the DumpingGround. should be unique in ULB
          maxLength: 128
          minLength: 1
        ulbs:
          description: ULB's
          type: array
          items: 
            $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/tenant/contracts/v1.0.1.yml#/definitions/Tenant'
          minItems: 1
        siteDetails:
          $ref: '#/definitions/SiteDetails'
        isProcessingSite:
          type: boolean
        distanceFromProcessingSite:
          type: number
          format: double
        processingSite:
          $ref: '#/definitions/ProcessingSite'
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - name
      - ulbs
      - siteDetails
      
    ProcessingSite:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the DumpingGround
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the DumpingGround
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the DumpingGround. should be unique in ULB
          maxLength: 128
          minLength: 1
        siteDetails:
          $ref: '#/definitions/SiteDetails'
      required:
      - tenantId
      - name
      - siteDetails
      
    SiteDetails:
      type: object
      properties:
        location:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/egov-location/contracts/v1-0-0.yml#/definitions/Boundary'
        area:
          type: number
          format: double
          description: area of the Dumping Ground in Km^2. Shall be > 0
          minimum: 0
          exclusiveMinimum: true
        capacity:
          type: number
          format: double
          description: capacity of the Dumping Ground in Tonnes. Shall be > 0
          minimum: 0
          exclusiveMinimum: true
        address:
          type: string
          description: address of the Dumping Ground
          maxLength: 500
          minLength: 15
        latitude:
          type: number 
          format: double
          description: latitude of the Dumping Ground. e.g. 12.9716. Latitudes range from -90 to 90. Positive latitude is above the equator (N), and negative latitude is below the equator (S).
          example: 38.544907
        longitude:
          type: number 
          format: double
          description: longitude of the Dumping Ground. e.g. 77.5946. Longitudes range from -180 to 180. Positive longitude is east of the prime meridian, while negative longitude is west of the prime meridian
          example: -121.740517
        wasteTypes:
          description: waste types that can be dumped at dumping groundp or processing site. At least one waste type has to be selected
          type: array
          items: 
            $ref: '#/definitions/WasteType'
          minItems: 1
        mpcbAuthorisation:
          type: boolean 
          description: Option should be selected if dumping ground is authorized by MPCB (Maharashtra Pollution Control Board). By default keep it unchecked
        bankGuarantee:
          type: boolean 
          description: Option should be selected if Bank Guarantee is provided
        bankName:
          type: string
          description: name of the bank that has given guarantee. Enable this field Iff mpcbAuthorisation is checked.
          maxLength: 256
          minLength: 1
        bankValidityFrom:
          type: integer
          format: int64
          x-displayformat: 20/10/2017
          description: Enable this field Iff mpcbAuthorisation is checked.
        bankValidityTo:
          type: integer
          format: int64
          x-displayformat: 20/11/2017
          description: Enable this field Iff mpcbAuthorisation is checked. Shall be > bankValidityFrom
      required:
      - location
      - area
      - capacity
      - address
      - wasteTypes  
      
    Vendor:
      type: object
      properties:
        tenantId:
          type: string
          description: tenant id of the Vendor
          maxLength: 128
          minLength: 1
        vendorNo:
          type: string
          description: Vendor number of the Vendor. unique Vendor number shall be generated by the system
          maxLength: 128
          minLength: 0
        name:
          type: string
          description: Name of the Vendor
          maxLength: 256
          minLength: 1
        servicedLocations:
          type: array
          items:
           $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/egov-location/contracts/v1-0-0.yml#/definitions/Boundary'
           minItems: 1
        registrationNo:
          type: string
          description: registration number of the vendor. Should be unique 
          maxLength: 256
          minLength: 1
        supplier:
          $ref: '#/definitions/Supplier'
        agreementDocument:
          $ref: '#/definitions/Document'
        servicesOffered:
          type: array
          items: 
            $ref: "#/definitions/SwmProcess"
            minItems: 1
            format: multiSelectcheckbox
            description: list of various services provided by vendor. At least one service has to be selected. That can be "others" too.
        details:
          type: string
          description: provide details if selected service under "servicesOffered" is others. if "others" is selected in "servicesOffered" field, details are mandatory
          maxLength: 500
          minLength: 0
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - registrationNo
      - name
      - servicesOffered
      - servicedLocations
      
    VendorRequest:
      description: Contract class to receive request. Array of Vendor items are used in case of create, whereas single Vendor item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vendors:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/Vendor'
    VendorResponse:
      description: Contract class to send response. Array of Vendor items are used in case of search results or response for create, whereas single Vendor item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vendors:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/Vendor'
      
    Supplier:
      type: object
      properties:
        tenantId:
          type: string
          description: tenant id of the supplier
          maxLength: 128
          minLength: 0
        supplierNo:
          type: string
          description: unique number, generated by the system, assigned to every supplier
          maxLength: 128
          minLength: 0
        name:
          type: string
          description: Name of the supplier
          maxLength: 100
          minLength: 0
        agencyName:
          type: string
          description: Name of the supplier
          maxLength: 100
          minLength: 0
        mobileNo:
          type: string
          description: contact number of the supplier. Mobile phone number without +91. One number shall be assigned to one supplier name only
          maxLength: 10
          minLength: 0
        contactNo:
          type: string
          description: contact number of the supplier. Phone number without +91. One phone number shall be assigned to one supplier name only
          maxLength: 10
          minLength: 10
        faxNumber:
          type: string
          description: FAX number of the supplier
          maxLength: 0
          minLength: 10
        email:
          type: string
          description: email ID of the supplier. If entered, must contain @ and .
          maxLength: 256
          minLength: 7
        address:
          type: string
          description: address of the supplier
          maxLength: 500
          minLength: 10
        tinNumber:
          type: string
          description: TIN number of the supplier
          maxLength: 256
          minLength: 0
        gst:
          type: string
          description: GST of the supplier. Combination of contarctor name and GST should be unique
          maxLength: 256
          minLength: 1
        aadharNo:
          type: string
          description: aadharNo of the vendor. Combination of supplier name and Aadhar should be unique. 
          minimum: 0
          maximum: 999999999999
      required:
      - tenantId
      - email
      - contactNo
      - gst
      - address
    
    SwmProcess:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the Vendor
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the Vendor
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: defines various processes names, including others, under SWM . e.g. collection, disposal etc
          maxLength: 256
          minLength: 1
      required:
      - tenantId
      - name
    
    VendorContract:
      type: object
      description: This object captures contract information of a vendor. One vendor can have multiple contracts. However, two contracts CAN NOT be similar in all aspects. Either period of service shall be non-overlapping or services provided shall be different. 
      properties:
        tenantId:
          type: string
          description: tenant id of the Vendor
          maxLength: 128
          minLength: 1
        vendor:
          $ref: "#/definitions/Vendor"
          format: singleValueList
        contractNo:
          type: string
          description: contract number of the Contract. unique contract number shall be generated by the system
          maxLength: 128
          minLength: 6
        contractDate:
          type: integer
          format: int64
        contractPeriodFrom:
          type: integer
          format: int64
        contractPeriodTo:
          type: integer
          format: int64
          description: Shall be > contractPeriodFrom
        securityDeposit:
          type: number 
          format: double
          description: securityDeposit (in Rs.) provided by the Vendor
        paymentAmount:
          type: number 
          format: double
          description: payment amount (in Rs.) for contract.
        paymentTerms:
          $ref: '#/definitions/PaymentTerms'
          description: payment terms  of the Vendor Contract. At least one of the option has to be selected. Only one can be selected
          format: radios
        remarks:
          type: string
          description: ULB Admin can put remarks, if any
          maxLength: 500
          minLength: 0
        servicesOffered:
          type: array
          items: 
            $ref: "#/definitions/SwmProcess"
            minItems: 1
            format: multiSelectcheckbox
            description: list of various services provided by vendor. At least one service has to be selected. That can be "others" too.        
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - vendor
      - contractDate
      - contractPeriodFrom
      - contractPeriodTo
      - securityDeposit
      - paymentTerms
      - paymentAmount
      - servicesOffered
      
    VendorContractRequest:
      description: Contract class to receive request. Array of VendorContract items are used in case of create, whereas single VendorContract item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vendorContracts:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/VendorContract'
    
    VendorContractResponse:
      description: Contract class to send response. Array of VendorContract items are used in case of search results or response for create, whereas single VendorContract item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vendorContracts:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/VendorContract'
    
    PaymentTerms:
      type: object
      description: This object captures payment frequency. e.g Monthly, quarterly 
      properties:
        tenantId:
          type: string
          description: tenant id of the Vendor
          maxLength: 128
          minLength: 1
        noOfDays:
          type: integer
          format: int64
          description: length of payment period in days. e.g. 30
        label:
          type: string
          description: define payment term name. e.g. yearly
          maxLength: 20
          minLength: 1
      required:
      - tenantId
      - label
      
    ShiftType:
      type: object
      properties:
        tenantId:
          type: string
          description: tenant id of the ShiftType
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the ShiftType
          maxLength: 128
          minLength: 1
        code:
          type: string
          description: code of the ShiftType
          maxLength: 128
          minLength: 1
      required:
      - tenantId
      - name
      - code
      
    Shift:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the Shift
          maxLength: 256
          minLength: 1
        name:
          type: string
          description: Unique Identifier of the Shift
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the Shift
          maxLength: 128
          minLength: 1
        shiftType:
          $ref: '#/definitions/ShiftType'
        department:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/egov-common-masters/contracts/v1-0-0.yml#/definitions/Department'
        designation:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/hr/contracts/hr-masters/v1-0-0.yml#/definitions/Designation'
        shiftStartTime:
          type: integer
          format: int64
          description: Start time should not overlap with other shifts. e.g. Shift 1 is from 6 AM to 2:30 PM. Another shift can NOT have start time between 6 AM and 2:30 PM.
        shiftEndTime:
          type: integer
          format: int64
          description: Should not overlap with any other shift timing
        lunchStartTime:
          type: integer
          format: int64
          description: should be between shift start and end time
        lunchEndTime:
          type: integer
          format: int64
          description: should be between shift start and end time. should be > lunchStartTime
          # Shift start < lunch start < lunch end < Shift end
        graceTimeFrom:
          type: integer
          format: int64
          description: should be between shift start and end time.
        graceTimeTo:
          type: integer
          format: int64
          description: should be between shift start and end time. should be > graceTimeFrom
        remarks:
          type: string
          description: ULB admin can enter remarks if any
          maxLength: 300
          minLength: 15
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - name
      - shiftType
      - department
      - designation
      - shiftStartTime
      - shiftEndTime
      - lunchStartTime
      - lunchEndTime
      - graceTimeFrom
      - graceTimeTo
      
    CollectionType:
      type: object
      properties:
        tenantId:
          type: string
          description: tenant id of the CollectionType
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the CollectionType. e.g. Door to Door, community bin
          maxLength: 128
          minLength: 1
        code:
          type: string
          description: code of the CollectionType. e.g. Door to Door, community bin
          maxLength: 128
          minLength: 1
      required:
      - tenantId
      - name
      - code
      
    VehicleMaintenance:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the VehicleMaintenance
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the VehicleMaintenance
          maxLength: 128
          minLength: 1
        vehicle:
          $ref: "#/definitions/Vehicle"
          format: autoComplete
        # At least one of the maintenance parameter has to provided
        maintenanceUom:
          type: string
          maxLength: 5
          minLength: 2
          description: Service interval period definition in either KMs or Days
        maintenanceAfter:
          type: integer
          format: int64
          minimum: 1
          maximum: 20000
          description: Expected Days or KMs after which regular maintenance is due
        downtimeforMaintenanceUom:
          type: string
          maxLength: 5
          minLength: 3
          description: Downtime definition in either days or hours
        downtimeforMaintenance:
          type: integer
          format: int64
          minimum: 0
          maximum: 720
          exclusiveMinimum: true
          description: Expected downtime of Vehicle in days or hours due to regular maintenance.
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - maintenanceUom
      - maintenanceAfter
      - downtimeforMaintenanceUom
      - downtimeforMaintenance
      - vehicle
      
    VehicleMaintenanceRequest:
      description: Contract class to receive request. Array of VehicleMaintenance items are used in case of create, whereas single VehicleMaintenance item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vehicleMaintenances:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/VehicleMaintenance'
    
    VehicleMaintenanceResponse:
      description: Contract class to send response. Array of VehicleMaintenance items are used in case of search results or response for create, whereas single VehicleMaintenance item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vehicleMaintenances:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/VehicleMaintenance'
    
    
    Population:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the Population
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the Population
          maxLength: 128
          minLength: 1
        location:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/egov-location/contracts/v1-0-0.yml#/definitions/Boundary'
        ulb:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/tenant/contracts/v1.0.1.yml#/definitions/Tenant'
        censusYear:
          type: integer
          format: int64
          description: census Year
          maxLength: 4
          minLength: 1
        population:
          type: integer
          format: int64
          description: population according to census year
        garbageToBeCollected:
          type: number
          format: double
          description: Expected garbage (in tonnes) that is to be collected from the Ward and Zone based on Population. e.g 20.86
      required:
      - code
      - tenantId
      - location
      - censusYear
      - population
      - garbageToBeCollected
      
    RefillingPumpStation:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the RefillingPumpStation
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the RefillingPumpStation
          maxLength: 128
          minLength: 1
        location:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/egov-location/contracts/v1-0-0.yml#/definitions/Boundary'
          description: Location of Ward -> Zone -> Street -> Colony shall be captured
        name:
          type: string
          description: name of the RefillingPumpStation
          maxLength: 256
          minLength: 1
        typeOfPump:
          $ref: '#/definitions/OilCompany'
          format: singleValueList
          description: pump Type of the RefillingPumpStation. e.g. HP, Indian Oil etc
        remarks:
          type: string
          description: ULB Admin can enter remarks, if any
          maxLength: 300
          minLength: 15
        fuelTypes:
          type: array
          items: 
            $ref: '#/definitions/FuelType'
          minItems: 1
          format: multiValueList
          description: type of the fuel. e.g Petrol, Diesel etc. At least one option has to be selected
        quantity:
          type: number
          format: double
          description: quantity of the fuel.
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - location
      - name
      - typeOfPump
      - fuelTypes
      
    RefillingPumpStationRequest:
      description: Contract class to receive request. Array of RefillingPumpStation items are used in case of create, whereas single RefillingPumpStation item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        refillingPumpStations:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/RefillingPumpStation'
    
    RefillingPumpStationResponse:
      description: Contract class to send response. Array of RefillingPumpStation items are used in case of search results or response for create, whereas single RefillingPumpStation item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        refillingPumpStations:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/RefillingPumpStation'
    
    OilCompany:
      type: object
      description: This object captures name of Oil Companies operating petrol pumps. e.g Indian Oil 
      properties:
        code:
          type: string
          description: Unique Identifier of the Vendor
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the Vendor
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of oil OilCompany, operating petrol pumps. e.g. HP
          maxLength: 20
          minLength: 2
      required:
      - code
      - tenantId
      - name
    
    
    Toilet: 
      type: object
      description: details will be integrated with Assest Management. A unique ID for toilet shall be generated. Either Public or Community toilet option shall be selected.
      properties:
        code:
          type: string
          description: Unique Identifier of the Toilet
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the Toilet
          maxLength: 128
          minLength: 1
        publicToilet:
          type: boolean
          description: Option should be selected if toilet is public toilet. else will be considered as community toilet
        name:
          type: string
          description: name of the Toilet. Shall be unique within ULB
          maxLength: 128
          minLength: 1
        location:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/egov-location/contracts/v1-0-0.yml#/definitions/Boundary'
        latitude:
          type: number 
          format: double
          description: latitude of the toilet
        longitude:
          type: number 
          format: double
          description: longitude of the toilet. e.g. 77.5946. Longitudes range from -180 to 180. Positive longitude is east of the prime meridian, while negative longitude is west of the prime meridian
          example: -121.740517
        seatCount: 
          type: integer 
          format: int64
          description: number of seats in the toilet
          minimum: 0
          exclusiveMinimum: true
      required:
      - tenantId
      - name
      - location
      - seatCount
      
    VehicleSchedule:
      type: object
      properties:
        transactionNo:
          type: string
          description: Unique Identifier of the SanitationStaffTarget
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the SetVehicleSchedule
          maxLength: 128
          minLength: 1
        scheduledFrom:
          type: integer
          format: int64
          description: date picker is requireed. Should be current or future date
        scheduledTo:
          type: integer
          format: int64
          description: date picker is requireed. scheduledTo date should be > scheduledFrom
        route:
         $ref: "#/definitions/Route"
         format: autoComplete
        vehicle:
          $ref: "#/definitions/Vehicle"
        targetedGarbage:
          type: number
          format: double
          description: Expected Garbage (in tonnes) to be collected by vehicle.
          minimum: 1
          maximum: 75
      required:
      - tenantId
      - scheduledFrom
      - scheduledTo
      - route
      - vehicle
      - targetedGarbage
    
    VehicleScheduleRequest:
      description: Contract class to receive request. Array of VehicleSchedule items are used in case of create, whereas single VehicleSchedule item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vehicleSchedules:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/VehicleSchedule'
    
    VehicleScheduleResponse:
      description: Contract class to send response. Array of VehicleSchedule items are used in case of search results or response for create, whereas single VehicleSchedule item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vehicleSchedules:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/VehicleSchedule'
    
    SanitationStaffTarget:
      type: object
      properties:
        tenantId:
          type: string
          description: tenant id of the SanitationStaffTarget
          maxLength: 128
          minLength: 1
        targetNo:
          type: string
          description: System shall geneate unique target number
          maxLength: 256
          minLength: 1
        targetFrom:
          type: integer
          format: int64
          description: date picker is required. Should be current or future date
        targetTo:
          type: integer
          format: int64
          description: date picker is required. Should be > targetFrom date
        swmProcess:
          $ref: '#/definitions/SwmProcess'
          format: singleValueList
          description: Sanitary staff target is defined based on SWM process. However ,transportation is excluded from this list. Target will be assigned for Collection, Segregation and Disposal process.
        route:
          $ref: "#/definitions/Route"
          format: singleValueList
          description: Admin will select the route based on location selected.
        employee:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/hr/contracts/hr-employee/v1-0-0.yml#/definitions/Employee'
          description: sanitary staff name based on dept. and designation.
        collectionPoints:
          type: array
          items:
            $ref: '#/definitions/CollectionPoint'
          description: List of Collection points based on the route and location selected. Admin will be able to select one or more colection points
        targetedGarbage: 
          type: number
          format: double
          description: targeted Garbage (in tonnes) to be assigned to Sanitatary Staff. If process selected is segregation, this filed will be autofilled (Wet waste + Dry waste)
          minimum: 1
          maximum: 500000
        wetWaste: 
          type: number
          format: double
          description: targeted wet waste (in tonnes) to be assigned to Sanitatary Staff. Mandatory ONLY IF swmProcess selected is segregation. Else disbale this field
        dryWaste: 
          type: number
          format: double
          description: targeted dry waste (in tonnes) to be assigned to Sanitatary Staff. Mandatory ONLY IF swmProcess selected is segregation. Else disbale this field
        dumpingGround:
          $ref: '#/definitions/DumpingGround'
          description: Mandatory if process selected is disposal. Else disable this field 
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - targetFrom
      - targetTo
      - swmProcess
      - employee
      - targetedGarbage
      
    SanitationStaffTargetRequest:
      description: Contract class to receive request. Array of SanitationStaffTarget items are used in case of create, whereas single SanitationStaffTarget item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        sanitationStaffTargets:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/SanitationStaffTarget'
    
    SanitationStaffTargetResponse:
      description: Contract class to send response. Array of SanitationStaffTarget items are used in case of search results or response for create, whereas single SanitationStaffTarget item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        sanitationStaffTargets:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/SanitationStaffTarget'
    
    SanitationStaffSchedule:
      type: object
      description: Captures the schedule details of sanitary staff
      properties:
        tenantId:
          type: string
          description: tenant id of the SanitationStaffSchedule
          maxLength: 128
          minLength: 1
        transactionNo:
          type: string
          description: System generated unique transaction number for every schedule
          maxLength: 256
          minLength: 1
        sanitationStaffTarget:
          $ref: '#/definitions/SanitationStaffTarget'
          description: Staff target number shall be entered by admin. This will populate location details, collection points assigned to individual employee and also the period for which schedule is being prepared.   
        shift:
          $ref: '#/definitions/Shift'
          format: singleValueList
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - sanitationStaffTarget
      - shift
    
    SanitationStaffScheduleRequest:
      description: Contract class to receive request. Array of SanitationStaffSchedule items are used in case of create, whereas single SanitationStaffSchedule item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        sanitationStaffSchedules:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/SanitationStaffSchedule'
    
    SanitationStaffScheduleResponse:
      description: Contract class to send response. Array of SanitationStaffSchedule items are used in case of search results or response for create, whereas single SanitationStaffSchedule item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        sanitationStaffSchedules:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/SanitationStaffSchedule'
    
    VehicleFuellingDetails:
      type: object
      properties:
        tenantId:
          type: string
          description: tenant id of the VehicleFuellingDetails
          maxLength: 128
          minLength: 1
        transactionNo:
          type: string
          description: transactionNo. unique across the system. Shall be generated by system
          maxLength: 256
          minLength: 0
        transactionDate:
          type: integer
          format: int64
          description: transactionDate. By default set to current date
        vehicle:
          $ref: "#/definitions/Vehicle"
        vehicleReadingDuringFuelling:
          type: number 
          format: double
          description: vehicle Reading During Fueling
          minimum: 1
          maximum: 300000
        refuellingStation:
          $ref: "#/definitions/RefillingPumpStation"
          format: autocomplete
        fuelFilled:
          type: number
          format: double
          description: fuel Filled in the Vehicle in liters. e.g. 40.67
          minimum: 1
          maximum: 500
        totalCostIncurred:
          type: number 
          format: double
          description: total Cost of the fuel
        receiptNo: # We shall provide document upload to attached scan copy of receipt
          type: string
          description: receipt Number of receipt that contains fueling Details. Space not allowed
          maxLength: 256
          minLength: 1
        receiptDate:
          type: integer
          format: int64
          description: receipt date of the Fuel receipt. e.g. 20/10/2017
        receiptCopy:
          $ref: '#/definitions/Document'
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - transactionDate
      - vehicle
      - vehicleReadingDuringFuelling
      - refuellingStation
      - fuelFilled
      - totalCostIncurred
      - receiptNo
      - receiptDate
    
    VehicleFuellingDetailsRequest:
      description: Contract class to receive request. Array of VehicleFuellingDetails items are used in case of create, whereas single VehicleFuellingDetails item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vehicleFuellingDetails:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/VehicleFuellingDetails'
            
    VehicleFuellingDetailsResponse:
      description: Contract class to send response. Array of VehicleFuellingDetails items are used in case of search results or response for create, whereas single VehicleFuellingDetails item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vehicleFuellingDetails:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/VehicleFuellingDetails'  
    
    VehicleMaintenanceDetails:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the VehicleMaintenanceDetails
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the VehicleMaintenanceDetails
          maxLength: 128
          minLength: 1
        isScheduled:
          type: boolean
          format: checkbox
          description: Maintenance is either scheduled or non-scheduled. This field captures whether it is sheduled or non-scheduled maintenance / repair. Not selected corresponds to non-scheduled
        
        # ONLY one of "isMaintenance" OR "isRepair" can be selected. At least one of the two shall be selected  
          
        maintenanceType:
          type: string
          maxLength: 128
          minLength: 1
          description: select if it is maintenance value will be MAINTENANCE if it is repair then value will be REPAIR
        vehicle:
          $ref: "#/definitions/Vehicle"
          description: Registration number of the vehicle whose details are being captured
        actualMaintenanceDate:
          type: integer
          format: int64
          description: actual Date when the maintenance / repair took place
        vehicleScheduledMaintenanceDate:
          type: integer
          format: int64
          description: vehicle Scheduled Maintenance Date. Populate this field from Vehicle Maintenance Master. Applicable ONLY in case of scheduled activities. Else this filed is not required# We need to define a formula to calculate this date
        costIncurred:
          type: number 
          format: double
          description: cost Incurred of the VehicleMaintenanceDetails
        vehicleDowntimeActual:
          type: number
          format: double
          description: Actual Downtime (in hours) due to planned maintenance 
        vehicleDownTimeActualUom:
          type: string
          description: To identify downtime measurement.Possible values hours/days
        vehicleReadingDuringMaintenance:
          type: number
          format: double
          description: vehicle reading during maintenance
        remarks:
          type: string
          description: remarks by ULB Admin
          maxLength: 300
          minLength: 15
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - maintenanceType
      - isScheduled
      - vehicle
      - actualMaintenanceDate
      - costIncurred
      - vehicleDowntimeActual
      - vehicleDownTimeActualUom
      - vehicleReadingDuringMaintenance
      
    VehicleMaintenanceDetailsRequest:
      description: Contract class to receive request. Array of VehicleMaintenanceDetails items are used in case of create, whereas single VehicleMaintenanceDetails item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vehicleMaintenanceDetails:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/VehicleMaintenanceDetails'
    VehicleMaintenanceDetailsResponse:
      description: Contract class to send response. Array of VehicleMaintenanceDetails items are used in case of search results or response for create, whereas single VehicleMaintenanceDetails item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vehicleMaintenanceDetails:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/VehicleMaintenanceDetails'
    
    VehicleTripSheetDetails: 
      type: object
      description: SWM will integrate with vehicle tracking system too for live tracking and to check the route taken on previous days. System will generate unique TripID
      properties:
        tenantId:
          type: string
          description: tenant id of the VehicleTripSheetDetails
          maxLength: 128
          minLength: 1
        route:
          $ref: '#/definitions/Route'
          format: autoComplete
          description: this field identifies the route that a garbage vehicle was assigned to.
        tripNo:
          type: string
          description: system generated unique trip number for every trip.
          maxLength: 128
          minLength: 1
        vehicle:
          $ref: '#/definitions/Vehicle'
          description: List of vehicles that are assigned against selected route under "routeTaken"
        tripStartDate:
          type: integer
          format: int64
          description: start date of the trip.  
        tripEndDate:
          type: integer
          format: int64
          description: end date of the trip. Should be >= tripStartDate
        inTime:
          type: integer
          format: int64
          description: time at which vehicle enters the dumping ground. Mandatory if ending point is Dumping ground
        outTime:
          type: integer
          format: int64
          description: time at which vehicle leaves the dumping ground. Mandatory if ending point is Dumping ground
        entryWeight:
          type: number
          format: double
          description: Weight of vehicle at the time of entering dumping ground. Mandatory if ending point is Dumping ground
        exitWeight:
          type: number
          format: double
          description: Weight of vehicle at the time of leaving dumping ground. Mandatory if ending point is Dumping ground
        garbageWeight:
          type: number
          format: double
          description: This is the difference of entryWeight and  exitWeight.          
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - route
      - vehicle
      - tripStartDate
      - tripEndDate
      
    VehicleTripSheetDetailsRequest:
      description: Contract class to receive request. Array of VehicleTripSheetDetails items are used in case of create, whereas single VehicleTripSheetDetails item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vehicleTripSheetDetails:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/VehicleTripSheetDetails'
    VehicleTripSheetDetailsResponse:
      description: Contract class to send response. Array of VehicleTripSheetDetails items are used in case of search results or response for create, whereas single VehicleTripSheetDetails item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vehicleTripSheetDetails:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/VehicleTripSheetDetails'
    
    SourceSegregation:
      type: object
      properties:
        code:
          type: string
          description: Unique Identifier of the SourceSegregation
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the SourceSegregation
          maxLength: 128
          minLength: 1
        dumpingGround:
          $ref: '#/definitions/DumpingGround'
          description: With the name of dumping ground, display the location data Ward / Zone/ street etc on GUI
        ulb:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/tenant/contracts/v1.0.1.yml#/definitions/Tenant'
          description: Code of ULB
        sourceSegregationDate:
          type: integer
          format: int64
          description: date picker required. date on which Segregation was done. By default make it system date
        collectionDetails:
          type: array
          items:
            $ref: "#/definitions/CollectionDetails"
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - dumpingGround
      - ulb
      - sourceSegregationDate
      - collectionDetails
      
    CollectionDetails:
      type: object
      properties:
        id:
          type: string
          description: Unique Identifier of the CollectionDetails
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the CollectionDetails
          maxLength: 128
          minLength: 1
        collectionType:
          $ref: "#/definitions/CollectionType"
        wetWasteCollected:
          type: number
          format: double
          description: quantity of wet waste collected (in tonnes)
          minimum: 0
        dryWasteCollected:
          type: number
          format: double
          description: quantity of dry waste collected (in tonnes)
          minimum: 0
      required:
      - tenantId
      - collectionType
      - wetWasteCollected
      - dryWasteCollected
      
    SourceSegregationRequest:
      description: Contract class to receive request. Array of SourceSegregation items are used in case of create, whereas single SourceSegregation item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        sourceSegregations:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/SourceSegregation'
    SourceSegregationResponse:
      description: Contract class to send response. Array of SourceSegregation items are used in case of search results or response for create, whereas single SourceSegregation item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        sourceSegregations:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/SourceSegregation'
      
    VendorPaymentDetails:
      type: object
      properties:
        paymentNo:
          type: string
          description: Unique Identifier of the VendorPaymentDetails and it is system generated.
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the VendorPaymentDetails
          maxLength: 128
          minLength: 1
        vendorContract:
          $ref: "#/definitions/VendorContract"
          format: singleValueList
        invoiceNo:
          type: string
          description: invoice Number. 
          maxLength: 256
          minLength: 1
        invoiceDate:
          type: integer
          format: int64
          description: Date on which invoice was created.
        fromDate:
          type: integer
          format: int64
          description: service period start date for which invoice is being submitted
        toDate:
          type: integer
          format: int64
          description: service period end date for which invoice is being submitted
        vendorInvoiceAmount:
          type: number 
          format: double
          description: vendor Invoice amount 
        documents:
          type: array
          description: List of Documents. Invoice or document supporting the invoice etc
          items:
            $ref: '#/definitions/Document'
        employee:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/hr/contracts/hr-employee/v1-0-0.yml#/definitions/Employee'
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - vendorContract
      - invoiceNo
      - fromDate
      - toDate
      - vendorInvoiceAmount
      
    PaymentDetails:
      type: object
      description: This object holds the details of payment instruments 
      properties:
        code:
          type: string
          description: Unique Identifier of the InstrumentDetails
          maxLength: 256
          minLength: 1
        tenantId:
          type: string
          description: tenant id of the InstrumentDetails
          maxLength: 128
          minLength: 1
        vendorPaymentDetails:
          $ref: "#/definitions/VendorPaymentDetails"
          description: It is unique number and it is same as paymentNo in Vendor Payment details. It will be used to link actual payment made against the invoice raised in Vendor payment details.
        voucherNumber:
          type: string
          description: Voucher Number. Shall be unique
          maxLength: 256
          minLength: 1
        voucherDate:
          type: integer
          format: int64
          description: date on which voucher was created. Shall be >= To Date in "Vendor payment details"
        instrumentType:
          type: string
          description: Payment Instrument Name. e.g. Demand draft, RTGS etc
          maxLength: 256
          minLength: 1
        instrumentNumber:
          type: string
          description: Instrument Number. Shall be unique across the platform. e.g. Cheque number, demad draft number etc
          maxLength: 256
          minLength: 1  
        instrumentDate:
          type: integer
          format: int64
          description: date on which payment instrument was created. e.g. Demand draft or Cheque creation date. Shall be >= Voucher Date
        amount:
          type: number 
          format: double
          description: Amount paid through the payment instrument
        pendingAmount:
          type: number 
          format: double
          description: This is the total pending amount calculated based on invoice amount.         
        bankName:
          type: string
          description: Bank Name from which payment is made. e.g. State bank of India
          maxLength: 256
          minLength: 1 
        branchName:
          type: string
          description: Bank Branch Name from which payment is made. e.g. Jayanagar, Bangalore
          maxLength: 256
          minLength: 1
        documents:
          type: array
          description: List of Documents. Document(s) supporting the payment
          items:
            $ref: '#/definitions/Document'
        auditDetails:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails'
      required:
      - tenantId
      - vendorPaymentDetails
      - voucherNumber
      - voucherDate
      - amount
      - instrumentType
      - instrumentDate
      - instrumentNumber
      - bankName
      
    Document:
      type: object
      description: An Object to hold the support document detail for a given Vendor Payment Details
      properties:
        id: 
          type: integer
          format: int64
          description: Unique Identifier of the Support Document
        tenantId:
          type: string
          description: tenant id of the document
          maxLength: 128
          minLength: 1
        name:
          type: string
          description: name of the document
          maxLength: 128
          minLength: 0
        refId:
          type: string
          maxLength: 256
          minLength: 1
          description: Unique Identifier of the reference id
        fileStoreId:
          type: string
          description: Unique Identifier of the FileStore
        comments:
          type: string
          maxLength: 1024
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
      - refId
      - tenantId
      - fileStoreId
      
    VendorPaymentDetailsRequest:
      description: Contract class to receive request. Array of VendorPaymentDetails items are used in case of create, whereas single VendorPaymentDetails item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        vendorPaymentDetails:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/VendorPaymentDetails'
    VendorPaymentDetailsResponse:
      description: Contract class to send response. Array of VendorPaymentDetails items are used in case of search results or response for create, whereas single VendorPaymentDetails item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        vendorPaymentDetails:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/VendorPaymentDetails'
            
    PaymentDetailsRequest:
      description: Contract class to receive request. Array of PaymentDetails items are used in case of create, whereas single PaymentDetails item is used for update
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        PaymentDetails:
          description: Used for search result and create only
          type: array
          items:
            $ref: '#/definitions/PaymentDetails'
    PaymentDetailsResponse:
      description: Contract class to send response. Array of PaymentDetails items are used in case of search results or response for create, whereas single PaymentDetails item is used for update
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        PaymentDetails:
          description: Used for search result and create only
          type: array
          maximum: 500
          items:
            $ref: '#/definitions/PaymentDetails'

parameters:
    pageSize:
      name: pageSize
      type: integer
      format: int32
      in: query
      description: Number of records in a per page in  the Search Results, Default value is 20
    
    offSet:
      name: offSet
      type: integer
      format: int32
      in: query
      description: page number of the Search Results, Default value is 0














