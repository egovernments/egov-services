  swagger: '2.0'
  info:
    version: 1.0.0
    title: Water Charges Management Service (WCMS) Master APIs
    description: |
      APIs available for WCMS Masters and mapping between two masters
        - Create, Update and Search for CategoryType
        - Create, Update and Search for PipeSize
        - Create, Update and Search for Donation
        - Create, Update and Search for DocumentType
        - Create, Update and Search for SourceType
        - Create, Update and Search for SupplyType
        - Search for ConnectionType
        - Search for ApplicationType
        - Create, Update and Search for mapping between PropertyType-CategoryType
        - Create, Update and Search for mapping between PropertyType-PipeSize
        - Create, Update and Search for mapping between PropertyType-UsageType
        - Create, Update and Search for mapping between DocumentType-ApplicationType
        - Create, Update and Search for Storage Reservoir Master
        - Create, Update and Search for Water Treatment Plant Master
    contact:
      name: Rishi
      email: rishi@egovernments.org
  schemes:
    - https
  basePath: '/wcms/masters/[API endpoint]'
  x-common-path: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml'

  paths:
    /categorytype/_create:
      post:
        summary: Create new category type.
        description: API to create new category type
        tags:
          - Category Type
        parameters:
          - name: Category Type
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/CategoryTypeReq'
        responses:
          '201':
            description: Category Type created successfully.
            schema:
              $ref: "#/definitions/CategoryTypeRes"
          '400':
            description: Category Type creation failed.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /categorytype/{code}/_update:
      parameters:
       - $ref: "#/parameters/updateCode"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - Category Type
        parameters:
          - name: Category Type
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/CategoryTypeReq'
        responses:
          '200':
            description: Category Type updated successfully.
            schema:
              $ref: "#/definitions/CategoryTypeRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /categorytype/_search:
      post:
        summary: searches existing values
        tags:
          - Category Type
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/code"
          - $ref: "#/parameters/name"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/CategoryTypeRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
  
  # PipeSize  API
    /pipesize/_create:
      post:
        summary: Create new pipe size.
        description: API to create new pipe size.
        tags:
          - Pipe Size
        parameters:
          - name: Pipe Size
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/PipeSizeReq'
        responses:
          '201':
            description: Pipe size created sucessfully.
            schema:
              $ref: "#/definitions/PipeSizeRes"
          '400':
            description: Invalid input.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /pipesize/{code}/_update:
      parameters:
       - $ref: "#/parameters/updateCode"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - Pipe Size
        parameters:
          - name: Pipe Size
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/PipeSizeReq'
        responses:
          '200':
            description: Pipe size updated successfully.
            schema:
              $ref: "#/definitions/PipeSizeRes"
          '400':
            description: Update Pipe Size failed.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /pipesize/_search:
      post:
        summary: searches existing values
        tags:
          - Pipe Size
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/code"
          - $ref: "#/parameters/sizeInMilimeter"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/PipeSizeRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
  
  # Document Type API
    /documenttype/_create:
      post:
        summary: creates different types of allowed document type
        description: allowed document type
        tags:
          - DocumentType
        parameters:
          - name: DocumentType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/DocumentTypeReq'
        responses:
          '201':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/DocumentTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /documenttype/{code}/_update:
      parameters:
      - $ref: "#/parameters/updateCode"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - DocumentType
        parameters:
          - name: DocumentType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/DocumentTypeReq'
        responses:
          '200':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/DocumentTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /documenttype/_search:
      post:
        summary: searches existing values
        tags:
          - DocumentType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/code"
          - $ref: "#/parameters/name"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/DocumentTypesRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
  
  # Source Type API
    /sourcetype/_search:
      post:
        summary: searches existing values
        tags:
          - SourceType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/code"
          - $ref: "#/parameters/name"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/SourceTypesRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sourcetype/_create:
      post:
        summary: creates different types of allowed Source Type
        description: allowed water source type
        tags:
          - SourceType
        parameters:
          - name: SourceType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/SourceTypeReq'
        responses:
          '201':
            description: SupplyType created sucessfully
            schema:
              $ref: "#/definitions/SourceTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /sourcetype/{code}/_update:
      parameters:
      - $ref: "#/parameters/updateCode"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - SourceType
        parameters:
          - name: SourceType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/SourceTypeReq'
        responses:
          '200':
            description: SupplyType Updated sucessfully
            schema:
              $ref: "#/definitions/SourceTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    # Supply Type API
    /supplytype/_search:
      post:
        summary: searches existing values
        tags:
          - SupplyType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/code"
          - $ref: "#/parameters/name"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/SupplyTypesRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /supplytype/_create:
      post:
        summary: creates different types of allowed supply Type
        description: allowed water supply type
        tags:
          - SupplyType
        parameters:
          - name: SupplyType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/SupplyTypeReq'
        responses:
          '201':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/SupplyTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /supplytype/{code}/_update:
      parameters:
      - $ref: "#/parameters/updateCode"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - SupplyType
        parameters:
          - name: SupplyType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/SupplyTypeReq'
        responses:
          '200':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/SupplyTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    # Application Type API
    /master/_getapplicationtypes:
      post:
        summary: Get all existing Application Type values
        tags:
          - ApplicationType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/CommonEnumRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
    
    # Reservoir Types API
    /master/_getreservoirtypes:
      post:
        summary: Get all existing Reservoir Type values
        tags:
          - ReservoirType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/CommonEnumRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    # Plant Types API
    /master/_getplanttypes:
      post:
        summary: Get all existing Plant Type values
        tags:
          - PlantType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/CommonEnumRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    
              
  # Property Type Category Type API
    /propertytype-categorytype/_create:
      post:
        summary: creates mapping between property type and category type
        description: allows mapping between two types.
        tags:
          - PropertyTypeCategoryType
        parameters:
          - name: PropertyTypeCategoryType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/PropertyTypeCategoryTypeReq'
        responses:
          '201':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/PropertyTypeCategoryTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /propertytype-categorytype/{id}/_update:
      parameters:
      - $ref: "#/parameters/updateId"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - PropertyTypeCategoryType
        parameters:
          - name: PropertyTypeCategoryType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/PropertyTypeCategoryTypeReq'
        responses:
          '200':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/PropertyTypeCategoryTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /propertytype-categorytype/_search:
      post:
        summary: searches existing values
        tags:
          - PropertyTypeCategoryType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/propertyTypeName"
          - $ref: "#/parameters/categoryTypeName"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/PropertyTypeCategoryTypesRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
  # Property Type PipeSize Type API
    /propertytype-pipesize/_create:
      post:
        summary: creates mapping between property type and pipe size 
        description: allows mapping between two types.
        tags:
          - PropertyTypePipeSize
        parameters:
          - name: PropertyTypePipeSize
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/PropertyTypePipeSizeReq'
        responses:
          '201':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/PropertyTypePipeSizeRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /propertytype-pipesize/{id}/_update:
      parameters:
      - $ref: "#/parameters/updateId"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - PropertyTypePipeSize
        parameters:
          - name: PropertyTypePipeSize
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/PropertyTypePipeSizeReq'
        responses:
          '200':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/PropertyTypePipeSizeRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /propertytype-pipesize/_search:
      post:
        summary: searches existing values
        tags:
          - PropertyTypePipeSize
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/propertyTypeName"
          - $ref: "#/parameters/pipeSizeInMM"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/PropertyTypePipeSizeRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
  # Property Type Usage Type API
    /propertytype-usagetype/_create:
      post:
        summary: creates mapping between property type and usage type
        description: allows mapping between two types.
        tags:
          - PropertyTypeUsageType
        parameters:
          - name: PropertyTypeUsageType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/PropertyTypeUsageTypeReq'
        responses:
          '201':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/PropertyTypeUsageTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /propertytype-usagetype/{id}/_update:
      parameters:
      - $ref: "#/parameters/updateId"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - PropertyTypeUsageType
        parameters:
          - name: PropertyTypeUsageType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/PropertyTypeUsageTypeReq'
        responses:
          '200':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/PropertyTypeUsageTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /propertytype-usagetype/_search:
      post:
        summary: searches existing values
        tags:
          - PropertyTypeUsageType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/propertyTypeName"
          - $ref: "#/parameters/usageTypeName"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/PropertyTypeUsageTypesRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    # Donation  API
    /donation/_create:
      post:
        summary: Create new donation .
        description: API to create new donation.
        tags:
          - Donation
        parameters:
          - name: Donation
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/DonationReq'
        responses:
          '201':
            description: Donation created sucessfully.
            schema:
              $ref: "#/definitions/DonationRes"
          '400':
            description: Invalid input.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
          
    /donation/{id}/_update:
      parameters:
       - $ref: "#/parameters/updateId"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - Donation
        parameters:
          - name: Donation
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/DonationRes'
        responses:
          '200':
            description: Donation updated successfully.
            schema:
              $ref: "#/definitions/DonationRes"
          '400':
            description: Update Donation failed.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /donation/_search:
      post:
        summary: searches existing values
        tags:
          - Donation
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/propertyTypeName"
          - $ref: "#/parameters/usageTypeName"
          - $ref: "#/parameters/categoryTypeName"
          - $ref: "#/parameters/pipeSizeInMM"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/DonationRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
   #  Storage Reservoir Master API
    /storagereservoir/_create:
      post:
        summary: Create new storage reservoir.
        description: API to create new storage reservoir.
        tags:
          - Storage Reservoir
        parameters:
          - name: Storage Reservoir
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/StorageReservoirReq'
        responses:
          '201':
            description: Storage reservoir created sucessfully.
            schema:
              $ref: "#/definitions/StorageReservoirRes"
          '400':
            description: Invalid input.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /storagereservoir/{id}/_update:
      parameters:
       - $ref: "#/parameters/updateId"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - Storage Reservoir
        parameters:
          - name: Storage Reservoir
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/StorageReservoirRes'
        responses:
          '200':
            description: Storage reservoir updated successfully.
            schema:
              $ref: "#/definitions/StorageReservoirRes"
          '400':
            description: Update Storage Reservoir failed.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /storagereservoir/_search:
      post:
        summary: searches existing values
        tags:
          - Storage Reservoir
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/reservoirType"
          - $ref: "#/parameters/name"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/StorageReservoirRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    #  Water Treatment Plant Master API
    /treatmentplant/_create:
      post:
        summary: Create new Water treatment plant.
        description: API to create Water treatment plant.
        tags:
          - Treatment Plant
        parameters:
          - name: Treatment Plant
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/TreatmentPlantReq'
        responses:
          '201':
            description: Treatment Plant created sucessfully.
            schema:
              $ref: "#/definitions/TreatmentPlantRes"
          '400':
            description: Invalid input.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
        
    /treatmentplant/_update:
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - Treatment Plant
        parameters:
          - name: Treatment Plant
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/TreatmentPlantReq'
        responses:
          '200':
            description: Treatment Plant updated successfully.
            schema:
              $ref: "#/definitions/TreatmentPlantRes"
          '400':
            description: Update Treatment Plant failed.
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
    /treatmentplant/_search:
      post:
        summary: searches existing values
        tags:
          - Treatment Plant
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/plantType"
          - $ref: "#/parameters/name"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/TreatmentPlantRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
      
   
              
   # Document Type Vs Application Type API
    /documenttype-applicationtype/_create:
      post:
        summary: creates mapping between document type and application type
        description: API to create Application Type and required document mapping.
        tags:
          - DocumentTypeApplicationType
        parameters:
          - name: DocumentTypeApplicationType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/DocumentTypeApplicationTypeReq'
        responses:
          '201':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/DocumentTypeApplicationTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /documenttype-applicationtype/{id}/_update:
      parameters:
      - $ref: "#/parameters/updateId"
      post:
        summary: updates existing values
        description: updates existing values
        tags:
          - DocumentTypeApplicationType
        parameters:
          - name: DocumentTypeApplicationType
            in: body
            description: required parameters have to be populated
            required: true
            schema:
              $ref: '#/definitions/DocumentTypeApplicationTypeReq'
        responses:
          '200':
            description: Request acknowledged sucessfully
            schema:
              $ref: "#/definitions/DocumentTypeApplicationTypesRes"
          '400':
            description: Request failed
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'

    /documenttype-applicationtype/_search:
      post:
        summary: searches existing values
        tags:
          - DocumentTypeApplicationType
        parameters:
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/requestInfo"
          - $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/parameters/tenantId"
          - $ref: "#/parameters/id"
          - $ref: "#/parameters/documentType"
          - $ref: "#/parameters/applicationType"
        responses:
          200: 
            description: Successful response
            schema:
              $ref: '#/definitions/DocumentTypeApplicationTypesRes'
          400:
            description: Invalid input
            schema:
              $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ErrorRes'
              
  parameters:
    updateCode:
      name: code
      in: path
      required: true
      description: Unique identifier of the master to be updated.
      type: string
      minLength: 0
      maxLength: 20
      exclusiveMinimum: true
      exclusiveMaximum: false
    updateId:
      name: id
      in: path
      required: true
      description: Unique identifier of the master to be updated.
      type: integer
      format: int64
      exclusiveMinimum: true
      exclusiveMaximum: false
    id:
      name: id
      in: query
      description: unique identifier of object
      required: false
      type: integer
      format: int64

    code:
      name: code
      in: query
      description: human reabable code
      required: false
      type: string
      minLength: 0
      maxLength: 20
      
    name:
      name: name
      in: query
      description: human reabable name
      required: false
      type: string
      minLength: 0
      maxLength: 100
    
    sizeInMilimeter:
      name: sizeInMilimeter
      in: query
      type: number
      description: Unique H.S.C pipe size in milli-meters.
  
      
    propertyTypeName:
      name: propertyTypeName
      in: query
      description: unique name of property type
      required: false
      type: string

    categoryTypeName:
      name: categoryTypeName
      in: query
      description: unique name of category type
      required: false
      type: string
      
    pipeSizeInMM:
      name: pipeSizeInMM
      in: query
      description: unique sizeInMM of pipe size type
      required: false
      type: number
      
    usageTypeName:
      name: usageTypeName
      in: query
      description: unique name of usage type
      required: false
      type: string
    
    documentType:
      name: documentType
      in: query
      description: unique code of document type
      required: false
      type: string
      
    reservoirType:
      name: reservoirType
      in: query
      description: unique name of reservoir type
      required: false
      type: string
      
    plantType:
      name: plantType
      in: query
      description: unique name of plant type
      required: false
      type: string

    applicationType:
      name: applicationType
      in: query
      description: unique identifier of application type
      required: false
      type: string
      
  definitions:
  
  # Proprty Type and Category Type mapping definition
    PropertyTypeCategoryTypeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        propertyTypeCategoryType:
          $ref: '#/definitions/PropertyTypeCategoryType'
      required:
        - requestInfo
        - propertyTypeCategoryType
    
    PropertyTypeCategoryTypesRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        propertyTypeCategoryTypes:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/PropertyTypeCategoryType'
    
    PropertyTypeCategoryType:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: Unique identifier, typically defined by system and readonly.
        propertyTypeId:
          type: string
          description: Unique identifier of property type object
        categoryTypeId:
          type: integer
          format: int64
          description: unique identifier of category type object
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        tenantId:
          type: string
          description: Unique Identifier of the tenant 
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - propertyTypeId
        - categoryTypeId
        - active
        - tenantId
          
  # Proprty Type and PipeSize Type mapping definition
    PropertyTypePipeSizeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        propertyTypePipeSize:
          $ref: '#/definitions/PropertyTypePipeSize'
      required:
        - requestInfo
        - propertyTypePipeSize
    
    PropertyTypePipeSizeRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        propertyTypePipeSizes:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/PropertyTypePipeSize'
    
    PropertyTypePipeSize:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: Unique identifier, typically defined by system and readonly.
        propertyTypeId:
          type: string
          description: Unique identifier of property type object
        pipeSizeId:
          type: integer
          format: int64
          description: unique identifier of pipe size type object
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        tenantId:
          type: string
          description: Unique Identifier of the tenant
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - propertyTypeId
        - pipeSizeId
        - active
        - tenantId

  # Proprty Type and Usage Type mapping definition
    PropertyTypeUsageTypeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        propertyTypeUsageType:
          $ref: '#/definitions/PropertyTypeUsageType'
      required:
        - requestInfo
        - propertyTypeUsageType
    
    PropertyTypeUsageTypesRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        propertyTypeUsageTypes:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/PropertyTypeUsageType'
            
    PropertyTypeUsageType:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: Unique identifier, typically defined by system and readonly.
        propertyTypeId:
          type: string
          description: Unique identifier of property type object
        usageTypeId:
          type: integer
          format: int64
          description: unique identifier of property usage type object
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        tenantId:
          type: string
          description: Unique Identifier of the tenant 
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - propertyTypeId
        - usageTypeId
        - active
        - tenantId

  # Document Type and Application Type mapping definition
    DocumentTypeApplicationTypeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        documentTypeApplicationType:
          $ref: '#/definitions/DocumentTypeApplicationType'
      required:
        - requestInfo
        - documentTypeApplicationType
    
    DocumentTypeApplicationTypesRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        documentTypeApplicationTypes:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/DocumentTypeApplicationType'

    DocumentTypeApplicationType:
      type: object
      properties:
        id:
          type: integer
          format: int64
          description: Unique identifier, typically defined by system and readonly.
        documentTypeId:
          type: integer
          format: int64
          description: Unique identifier of document type object
        applicationType:
          type: string
          format: varchar
          description: unique identifier of application type object
        mandatory:
          type: boolean
          format: varchar
          description: TRUE for active Document Names and FALSE for inactive Document Names.
        active:
          type: boolean
          format: varchar
          description: TRUE for active Document Names and FALSE for inactive Document Names    
        tenantId:
          type: string
          description: Unique Identifier of the tenant 
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - documentTypeId
        - applicationType
        - mandatory
        - active
        - tenantId

    ApplicationType:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier of the entered type, typically defined by system.
        code:
          description: unique code this should be assigned by system.
          maxLength: 100
          enum:
            - ADDITION CONNECTION
            - CHANGE OF USE
            - CLOSING CONNECTION
            - HOLDING CONNECTION
            - NEW CONNECTION
            - REGULARIZATION CONNECTION
            - TITTLE TRANSFER
        name: 
          type: string
          description: human readable name should be assigned by system.
          maxLength: 100
        description: 
          type: string
          description: human readable description should be assigned by user.
          maxLength: 100
      required:
        - code
        - name
        
    PlantType:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier of the entered type, typically defined by system.
        code:
          description: unique code this should be assigned by system.
          maxLength: 100
          enum:
            - SANDGRAVEL
        name: 
          type: string
          description: human readable name should be assigned by system.
          maxLength: 100
        description: 
          type: string
          description: human readable description should be assigned by user.
          maxLength: 100
      required:
        - code
        - name
        
    ReservoirType:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier of the entered type, typically defined by system.
        code:
          description: unique code this should be assigned by system.
          maxLength: 100
          enum:
            - GROUNDSTORAGERESERVOIR
            - ELEVATEDSTORAGERESERVOIR
        name: 
          type: string
          description: human readable name should be assigned by system.
          maxLength: 100
        description: 
          type: string
          description: human readable description should be assigned by user.
          maxLength: 100
      required:
        - code
        - name
        
    CommonDataModel:
      type: object
      properties:
        key:
         type: string
         description : This field holds the name of the enumerator
        object:
         description: This field holds any object of enumerator
         enum: 
           - $ref: '#/definitions/ApplicationType'
           - $ref: '#/definitions/PlantType'
           - $ref: '#/definitions/ReservoirType'
           

    CommonEnumRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        DataModelList:
          type: array
          maximum: 10
          items:
            $ref: '#/definitions/CommonDataModel'
  
  # Water Source Type definitions
    SourceTypesRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        sourceTypes:
          type: array
          maximum: 10
          items:
            $ref: '#/definitions/SourceType'
            
    SourceTypeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        sourceType:
          $ref: '#/definitions/SourceType'
      required:
        - requestInfo
        - sourceType
    
    SourceType:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier, typically defined by system and readonly.
        code:
          type: string
          description: unique code this should be defined by system and readonly.
          maxLength: 20
        name: 
          type: string
          description: human readable name should be assigned by user.
          maxLength: 100
        description: 
          type: string
          description: human readable description should be assigned by user.
          maxLength: 250
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        tenantId:
          type: string
          description: Unique Identifier of the tenant
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - code
        - name
        - active
        - tenantId    

  # Water Supply Type definitions
    SupplyTypesRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        supplyTypes:
          type: array
          maximum: 10
          items:
            $ref: '#/definitions/SupplyType'
            
    SupplyTypeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        supplyType:
          $ref: '#/definitions/SupplyType'
      required:
        - requestInfo
        - supplyType
    
    SupplyType:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier, typically defined by system and readonly.
        code:
          type: string
          description: unique code this should be defined by system and readonly.
          maxLength: 20
        name: 
          type: string
          description: human readable name should be assigned by user.
          maxLength: 100
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        description: 
          type: string
          description: human readable description should be assigned by user.
          maxLength: 250
        tenantId:
          type: string
          description: Unique Identifier of the tenant
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - code
        - name
        - active
        - tenantId

  # Document Type definitions
    DocumentTypeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        documentType:
          $ref: '#/definitions/DocumentType'
      required:
        - requestInfo
        - documentType
    
    DocumentTypesRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        documentTypes:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/DocumentType'
          
    DocumentType:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier, typically defined by system and readonly.
        code:
          type: string
          description: unique code this should be defined by system and readonly.
          maxLength: 20
        name: 
          type: string
          description: human readable name should be assigned by user.
          maxLength: 100
        description: 
          type: string
          description: human readable description should be assigned by user.
          maxLength: 250
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        tenantId:
          type: string
          description: Unique Identifier of the tenant 
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - code
        - name
        - active
        - tenantId
  
  # PipeSize definitions
    PipeSizeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        pipeSize:
          $ref: '#/definitions/PipeSize'
      required:
        - requestInfo
        - pipeSize

    PipeSizeRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        pipeSizes:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/PipeSize'
  
    PipeSize:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier, typically defined by system and readonly.
        code:
          type: string
          description: unique code this should be defined by system and readonly.
          maxLength: 20
        sizeInMilimeter:
          type: number
          description: Unique H.S.C pipe size in milli-meters.
        sizeInInch:
          type: number
          description: H.S.C pipe size in inches.
        description: 
          type: string
          description: human readable description should be assigned by user.
          maxLength: 250
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        tenantId:
          type: string
          description: Unique Identifier of the tenant
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - code
        - sizeInMilimeter
        - active
        - tenantId
        
 # Donation definitions
    DonationRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        donations:
          type: array
          maximum: 10
          items:
            $ref: '#/definitions/Donation'
            
    DonationReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        donations:
          $ref: '#/definitions/Donation'
      required:
        - requestInfo
        - donations
      
    Donation:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier of the donation, typically defined by system and readonly.
        propertyTypeId:
          type: string
          description: Unique identifier of property type object
        usageTypeId:
          type: string
          description: unique identifier of property usage type object
        maxPipeSizeId:
          type: integer
          format: int64
          description: unique identifier of maximum pipe size type object
        minPipeSizeId:
          type: integer
          format: int64
          description: unique identifier of minimum pipe size type object
        categoryTypeId:
          type: integer
          format: int64
          description: unique identifier of category type object
        donationAmount:
          type: number
          description: donation amount.
        fromDate:
          type: string
          format: date
          description: from Date.
        toDate:
          type: string
          format: date
          description: to Date.
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        tenantId:
          type: string
          description: Unique Identifier of the tenant
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - propertyTypeId
        - usageTypeId
        - maxPipeSizeId
        - minPipeSizeId
        - categoryTypeId
        - donationAmount
        - fromDate
        - toDate
        - active
        - tenantId  
    

        
  # Category Type definitions
    CategoryTypeReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        categoryType:
          $ref: '#/definitions/CategoryType'
      required:
        - requestInfo
        - categoryType
    
    CategoryTypeRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        categoryTypes:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/CategoryType'
    
    CategoryType:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier, typically defined by system and readonly.
        code:
          type: string
          description: unique code this should be defined by system and readonly.
          maxLength: 20
        name: 
          type: string
          description: human readable name should be assigned by user.
          maxLength: 100
        description: 
          type: string
          description: human readable description should be assigned by user.
          maxLength: 250
        active:
          type: boolean
          format: varchar
          description: TRUE for active and FALSE for inactive.
        tenantId:
          type: string
          description: Unique Identifier of the tenant 
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - code
        - name
        - active
        - tenantId
        
  # Water treatment plant  Master definitions
    TreatmentPlantReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        treatmentPlants:
          $ref: '#/definitions/TreatmentPlant'
      required:
        - requestInfo
        - treatmentPlants
    
    TreatmentPlantRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        treatmentPlants:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/TreatmentPlant'
            
    TreatmentPlant:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier, typically defined by system and readonly.
        code:
          type: string
          description: unique code this should be defined by system and readonly.
          maxLength: 20
        name: 
          type: string
          description: Name of the plant
          maxLength: 100
        location: 
          type: string
          description: boundary location  where storage reservoir is located should be selected.
        zone: 
          type: string
          description: boundary zone drop down should be displayed.
        ward: 
          type: string
          description: boundary ward drop down should be displayed.
        plantCapacity:
          type: string
          description: Water storage capacity of the plant in million litres should be entered
          maxLength: 8
        plantType:
          type: string
          description: Type of the water plant 
          enum:
            - Sand gravel
        storageReservoir :
          type: string
          description: Storage Reservoir drop down should be displayed
        description:
          type: string
          description: human readable description should be assigned by user
          maxLength: 250
        tenantId:
          type: string
          description: Unique Identifier of the tenant 
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - name
        - code
        - location
        - zone
        - ward
        - plantCapacity
        - plantType
        - tenantId
  
  # Storage Reservoir Master definitions
    StorageReservoirReq:
      type: object
      properties:
        requestInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/RequestInfo'
        storageReservoir:
          $ref: '#/definitions/StorageReservoir'
      required:
        - requestInfo
        - storageReservoir
    
    StorageReservoirRes:
      type: object
      properties:
        responseInfo:
          $ref: 'https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/ResponseInfo'
        storageReservoirs:
          type: array
          maximum: 20
          items:
            $ref: '#/definitions/StorageReservoir'
            
    StorageReservoir:
      type: object
      properties:
        id:
         type: integer
         format: int64
         description : Unique identifier, typically defined by system and readonly.
        code:
          type: string
          description: unique code this should be defined by system and readonly.
          maxLength: 20
        reservoirType:
          type: string
          description: Type of the Reservoir	.
          enum:
            - Ground storage reservoir
            - Elevated storage reservoir
        name: 
          type: string
          description: Name of the Storage Reservoir.
          maxLength: 10
        location: 
          type: string
          description: boundary location  where storage reservoir is located should be selected.
        zone: 
          type: string
          description: boundary zone drop down should be displayed.
        ward: 
          type: string
          description: boundary ward drop down should be displayed.
        storageCapacity:
          type: string
          description: Water storage capacity of the plant in million litres should be entered
          maxLength: 8
        numberofconnection:
          type: string
          description: User should enter details of the distribution line as ID, diameter of pipeline and any other details for the entered respective option
          maxLength: 124
        numberofsublines:
          type: string
          description: User should enter details of the distribution line as ID, diameter of pipeline and any other details for the entered respective option
          maxLength: 124
        numberofmainlines:
          type: string
          description: User should enter details of the distribution line as ID, diameter of pipeline and any other details for the entered respective option
          maxLength: 124
        tenantId:
          type: string
          description: Unique Identifier of the tenant
          maxLength: 250
        auditDetails:
          $ref: "https://raw.githubusercontent.com/egovernments/egov-services/master/docs/common/contracts/v1-0-0.yml#/definitions/AuditDetails"
      required:
        - reservoirType
        - code
        - name
        - location
        - zone
        - ward
        - storageCapacity
        - tenantId
        
