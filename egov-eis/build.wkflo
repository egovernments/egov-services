def build(service_name, ci_image) {
    stage("Build"){
        docker.image(ci_image).inside {
            sh "cd ${service_name}; mvn clean test pmd:pmd verify package";
        }
    }
}

return this;
