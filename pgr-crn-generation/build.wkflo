def build(service_name, ci_image) {
    stage("Build"){
        docker.image("${ci_image}").inside {
            sh "cd ${service_name}; mvn -v; mvn clean deploy -s settings.xml -Dnexus.user=${env.nexus.user} -Dnexus.password=${env.nexus.password}";
        }
    }
}

return this;
