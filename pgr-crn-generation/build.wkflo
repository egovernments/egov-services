def build(service_name) {
    stage("Build"){
        docker.image("${ci_image}").inside {
            sh "cd ${service_name}; mvn -v; mvn clean package -s settings.xml";
        }
    }
}

return this;