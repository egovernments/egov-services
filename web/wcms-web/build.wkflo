def react_ci_image = "egovio/ci-react:0.0.1"

def build(path, ci_image) {
    stage("Build") {
        docker.image(react_ci_image).inside {
            sh "cd ${path} && npm install && npm run build"
        }
    }
}

return this;
